# Logs
logs
*.local
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

.pnpm-store/
node_modules/
coverage/
/data/
dist/
dist-ssr/

# Claude Code 本机权限（.claude/skills 是指向 .agents/skills 的跟踪符号链接）
.claude/settings.local.json

# Skills 真相源是 .agents/skills/；勿再往这里放（Cursor 会与 .agents 双扫重复加载）
.cursor/skills/

# Editor directories and files
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# SQLite database
*.db
*.db-shm
*.db-wal
*.db.backup

# PostgreSQL backups
postgres-backup*.sql

# db-remote.sh pull 下载的备份产物（含敏感数据，勿提交）
/backups/

# Environment variables
.env
.env.local
.env.*.local
.env.development
.env.production
.env.test
.env.edge
# 遗留：历史 SSH 隧道 env，勿再新建
.env.tunnel

# Release
release/
*.tar.gz

# rewindom export backup (transient)
.cache/
