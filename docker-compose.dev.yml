# 本地开发依赖：PostgreSQL + Redis
# 应用仍在宿主机运行：pnpm db:up && pnpm dev
#
# 若本机 Postgres 未占用 5432，可将 POSTGRES_PORT 改为 5432 并同步 DATABASE_URL

services:
  postgres:
    image: postgres:16-alpine
    container_name: rewindom-dev-postgres
    environment:
      POSTGRES_USER: rewindom
      POSTGRES_PASSWORD: ${DB_PASSWORD:-dev-password}
      POSTGRES_DB: rewindom
    ports:
      - "${POSTGRES_PORT:-5433}:5432"
    volumes:
      - rewindom_pg:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U rewindom -d rewindom"]
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    container_name: rewindom-dev-redis
    ports:
      - "${REDIS_PORT:-6379}:6379"
    volumes:
      - rewindom_redis:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  rewindom_pg:
  rewindom_redis:
