---
description: 项目核心代码规范（命名、API、TypeScript、依赖）
alwaysApply: true
---

# rewindom - 代码规范

## 命名规范

- **数据库 / API / 共享类型 / 列表 URL 查询参数**: `snake_case`（详见 `field-naming` rule）
- **TypeScript 变量/函数**: `camelCase`
- **TypeScript 类/接口**: `PascalCase`
- **常量**: `UPPER_SNAKE_CASE`
- **文件命名**: `kebab-case.ts`
- **React 路由 path 参数**: camelCase（如 `:documentId`）

## 数据结构约定

所有数据结构使用 JSON 格式，字段使用英文 `snake_case` 命名：

```typescript
interface Document {
  id: string;
  title: string;
  content_type: string;
  status: DocumentStatus;
  created_at: Date;
}
```

## API 规范

### 响应格式

```typescript
// 成功 (HTTP 200)
{ data: T }

// 列表分页 (HTTP 200)
{ data: T[], meta: { total, page, page_size } }

// 错误 (HTTP 4xx/5xx)
{ error: string, code?: string }
```

**列表排序查询参数**（与 URL / `list-url-params` 一致）：

- `sort_by`：列字段名，`snake_case`，须与服务端排序白名单 / Prisma 字段一致
- `sort_dir`：`asc` | `desc`（慢查询历史接口为 `sort_order`；新接口统一 `sort_dir`）

### 请求规范

**禁止**在组件或 Context 中直接使用 `fetch`，必须使用 `@rewindom/client-kit` 中的 `api` 工具。

```typescript
import { api } from "@/lib/api";

const data = await api.get<Document>("/documents/:documentId");
await api.get("/users", { page, page_size, sort_by, sort_dir, search });
await api.post("/documents", { title, content }, true);
```

列表页排序、分页、筛选参数读写统一走 `@rewindom/client-kit/lib/list-url-params`（见 `frontend-page-structure` rule）。

**Token 管理：**

- 双 Token 模式：`access_token` + `refresh_token`
- localStorage: `app_access_token`、`app_refresh_token`
- `api.ts` 自动处理 401 并刷新 Token

**skipAuth 参数：** 登录/注册等公开接口传递 `skipAuth = true`

### HTTP 状态码

| 状态码 | 场景           |
| ------ | -------------- |
| 200    | 成功           |
| 201    | 创建成功       |
| 400    | 参数错误       |
| 401    | 未授权         |
| 404    | 资源不存在     |
| 409    | 资源冲突       |
| 500    | 服务器内部错误 |

## 错误处理

- 使用自定义错误类 `AppError` 继承 Error
- HTTP 状态码遵循上述 API 规范
- 后端统一通过 `error-handler.ts` 中间件处理

## 前端分层约定

- 通用数据 Hook（如 `useXxx`）只负责：请求调用、缓存失效、数据转换、错误上抛
- 页面/业务组件负责 UI 副作用：`toast`、`dialog`、`sheet` 开关、页面跳转
- **Dialog/Sheet 内聚（金标准）**：创建按钮（trigger）与含表单的 Sheet/Dialog 是同一高内聚组件（如 `NoteCreateSheet`），内含 trigger + 弹层 + mutation/toast；Page 只放置、禁止 `*Open` + 分散 Button/Dialog；行内用 `children` 作 trigger，列表详情在 Row/Table 内聚
- **一页一表单**：同一页面不要上下两张 `<form>`、两颗保存。密钥 / 收款通道收进 Sheet，页上留状态行 + 一张设置表单（金标准：`SiteBillingProviderStatusRow`、shop `ShopProviderStatusRow`）。详见 `frontend-page-structure` skill
- 默认禁止在通用 Hook 中直接调用 `toast.success/error` 等 UI 提示
- 仅在"强业务耦合且不会复用"的专用 Hook 中，才允许内聚 UI 副作用（需在代码评审中说明理由）
- **复杂页面**须按 `frontend-page-structure` rule 拆为 Page / Hook / Lib / Component（详见 Skill）

## 数据库操作

- 使用 Prisma ORM（Prisma 7）
- 复杂查询使用 `include` 和 `select`
- 事务使用 `$transaction`
- jsonb 列按内容检索用 `path` 过滤（如 `ErrorLog.request_body`），复杂 jsonpath 才降级到 `$queryRaw`

## 日志规范

- 使用 Fastify 内置日志
- 级别：`info` / `warn` / `error` / `debug`

## TypeScript 规范

- 开启 `strict: true`
- 避免 `any`，使用 `unknown` + 类型守卫
- 所有函数必须声明返回类型

## 导入导出规范

```typescript
// 1. Node.js 内置 → 2. 第三方 → 3. 项目内部
import path from "path";
import fastify from "fastify";
import { config } from "./config";
```

- **React 组件统一使用具名导出（named export）**，禁止 `export default`

## 变更与兼容性

**默认不做向后兼容**。除非用户在本次需求中明确要求保留兼容，否则改造时直接改到位：

- 废弃的字段、类型、设置项、i18n key、导出：**直接删**，不留 `@deprecated` 别名或 re-export
- 换了形状的数据：**直接换**，不写「读时兼容旧格式」的双分支
- 重命名：一次改完所有调用点，不保留旧名转发

理由是本仓库[独立维护](../../AGENTS.md#维护模式)、无外部消费者：兼容层没有真实受众，却会长期
留在代码里，让每个后来的人都要先分辨「这两条路哪条还活着」。

**唯一的例外是存量数据**：库里已经写进去的旧格式不能凭空失效。这种情况写**一次性升级函数**
（如 `chrome-upgrade.ts`），在解析入口把旧数据搬成新形状，而不是让渲染层长期两种格式都认。
升级函数要能判断「这确实是旧数据」，别把当前格式的合法形态误当旧数据反复改写。

## 注释规范

- 避免冗余区块注释（如 `{/* Document Header */}`）
- 仅在复杂业务逻辑、workaround、TODO、关键设计决策时使用注释

## 依赖管理

安装或更新依赖时，确保使用最新稳定版本：

```bash
ncu -u -w
pnpm install
```

**严禁**将依赖版本降级作为解决问题的手段。必须从代码层面定位根因并修复。
