---
description: be-water 架构与技术选型
alwaysApply: true
---

# 架构

```
Frontend (React SPA) → Fastify API (:3700) → PostgreSQL + Prisma
                                            └→ Redis（缓存 / BullMQ 队列）
```

多租户 SaaS **模块化单体**底座。Monorepo：

| 路径 | 角色 |
| --- | --- |
| `apps/server` / `apps/client` | 极薄组装层，只做启用模块的注册 |
| `packages/modules/<id>/` | 业务与基础设施模块（`shared` + `server` + `client` + 可选 `schema.prisma`） |
| `packages/server-kernel` | 后端内核：Prisma、认证、中间件、模块契约 |
| `packages/client-kit` / `packages/ui` | 前端 App Shell 与组件库 |
| `packages/shared` | 前后端共享类型与工具 |

内核与基础设施**不含业务领域代码**。详见 `docs/design/modular-architecture.md`。

## 技术栈

- **后端**: Fastify 5 + TypeScript 6 + Prisma 7 + PostgreSQL 16
- **前端**: React 19 + Vite 8 + React Router v7 + TanStack Query/Table + shadcn/ui + Tailwind CSS 4
- **认证**: JWT 双 Token（`access_token` + `refresh_token`），`api.ts` 自动刷新
- **任务队列**: BullMQ + ioredis
- **AI 能力**: OpenAI 兼容接口（`openai-client.ts` 只提供客户端与配置，不预设调用场景）

数据库只用原生 PostgreSQL，**没有启用任何扩展**。若将来要做向量检索，需同时
`CREATE EXTENSION vector` 并把 docker-compose 的镜像换成自带 pgvector 的
（`postgres:16-alpine` 不含该扩展）。

## 内置模块

`packages/modules/` 下，注册于 `apps/server/src/enabled-modules.ts`（infra → shell → 业务域）：

| 类型 | 模块 |
| --- | --- |
| 基础设施 | `user` 认证/JWT · `rbac` PBAC · `platform` 租户/套餐/配额 · `audit` 审计 · `notification` 站内通知 · `background-job` BullMQ 任务中心 · `error-log` / `slow-query` 可观测 |
| 示例业务 | `notes` 金标准 CRUD · `todos` 由 `gen:module` 生成的列表示例 |

新增模块：填 `MODULE.spec.yaml` → `pnpm gen:module` → 补 service → `pnpm check:modules`。

Skills：`create-module` · `extract-module` · `error-logging` · `frontend-page-structure` ·
`prisma-sync-fix` · `merge-migrations`。文档索引：`docs-reference` rule。
