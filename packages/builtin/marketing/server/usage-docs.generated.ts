/**
 * GENERATED — do not edit.
 * Source: `packages/builtin/marketing/docs/usage/<locale>/*.md`
 * Regenerate: `pnpm --filter @be-water/builtin assemble:usage-docs`
 */
import type { AppLocale } from "@be-water/shared";

export interface UsageDocFile {
  locale: AppLocale;
  /** 文件名即 slug（去 `.md`），frontmatter 提供 title/description/category/sort_order。 */
  filename: string;
  raw: string;
}

/** 默认租户文档库的初始内容，按 locale + 文件名排好序。 */
export const USAGE_DOCS: readonly UsageDocFile[] = [
  {
    locale: "en",
    filename: "build-site.md",
    raw: '---\ntitle: Building a site\ndescription: Starters, themes, header and footer, navigation\ncategory: build-operate\nsort_order: 50\n---\n\nEvery site has one marketing site made of pages, a theme, and a header and footer.\nThis is the path from starter template to a published site.\n\n## Starters\n\nA new site begins from a starter: one theme plus a set of pages.\n\n| Starter              | Good for            | Pages it creates                       |\n| -------------------- | ------------------- | -------------------------------------- |\n| Default marketing    | General purpose     | Home                                   |\n| Product site         | Product marketing   | Home + Pricing + About + Contact       |\n| Landing page         | Single-page landing | Home + Contact (a more spacious theme) |\n\nApply one from the site management page. Applying a starter **overwrites the content of\npages it owns**, but never deletes pages you created yourself.\n\n> The default marketing starter creating only a home page is deliberate. Whatever a\n> starter lays down, the first thing most people do is delete it. Adding a pricing page\n> from the page presets costs far less than cleaning up four pages you didn\'t want.\n\n## Themes\n\nA theme is a set of design tokens — colours, fonts, spacing, radii — that set the\nvisual tone:\n\n- Switch theme packs in site settings\n- Individual tokens can be overridden per site (say, only the brand colour)\n- The logo is a brand asset, configured separately from the theme\n\n## Header and footer\n\nHeader and footer are **site-level** sections shared by every page:\n\n- Header: logo, site name, navigation, sticky behaviour, layout\n- Footer: logo, blurb, copyright, link groups\n\n> Starters deliberately ship **no** header button. If you want a "Get started" call to\n> action, add it yourself in the header settings — a prepopulated button pointing at a\n> feature you haven\'t enabled is worse than no button.\n\n### Navigation links\n\nSelect the header and the "navigation links" panel on the right is exactly the row your\nvisitors see. Four kinds of entry are available:\n\n| Source           | Expands into                                          |\n| ---------------- | ----------------------------------------------------- |\n| Custom link      | One link you write, optionally with a submenu          |\n| All top-level pages | Every published top-level page; new pages join automatically |\n| Docs library     | The whole library, grouped by category (parent links to `/docs`) |\n| One doc category | Every published doc in that category                   |\n\nThe last three are **rules**, not fixed entries: what they expand into depends on your\ncontent, and the editor tells you what will show. When a rule expands to nothing, it is\nnot rendered at all — you never end up with an entrance that leads to an empty list.\n\nThe default configuration is "all top-level pages" plus "docs library". So the moment\nyou publish your first document, a Docs entry appears in the header; before that, it\nquietly isn\'t there. The docs index itself doesn\'t count as a top-level page, so it\nnever appears twice.\n\nEach footer column has its own list of links. To match the header, use "copy from\nheader" — it copies a snapshot, after which the two are edited independently.\n\n## Name and tagline\n\nThe site name and tagline appear in the header and in SEO metadata; both can be filled in per language. Replacing the\nstarter\'s placeholder copy is job number one.\n\n## Languages\n\nThe site supports multiple languages. The **primary language has no URL prefix** (it is\nthe canonical entrance for search engines); other languages live under `/{locale}/…`.\nPages and docs are both **stored per language**: one row per language for the same\npath, and those rows become a translation set automatically, which is what drives the\nlanguage switcher and hreflang tags.\n\nWhen a language has no documents at all, the docs library falls back to the primary\nlanguage rather than 404ing.\n\n## Publishing\n\nThe site has one master switch: while it is off visitors see a placeholder page.\n\nPages and documents each have their own draft/published state on top of that. **The\nmaster switch is only a master switch** — individual content still has to be published.\n\n## Next\n\n- Arrange page layout → [Pages and layout](/en/docs/manage-pages)\n- Manage documentation → [Documentation library](/en/docs/manage-docs)\n',
  },
  {
    locale: "en",
    filename: "faq.md",
    raw: '---\ntitle: FAQ\ndescription: Common questions about local setup, deployment, sites and modules\nsort_order: 120\n---\n\n## Local development\n\n### Why do `localhost:7300` and `127.0.0.1:7300` show different things?\n\nThey are two entrances. `localhost` is the product site (the default tenant),\n`127.0.0.1` is the platform console. This falls out of Host-based routing — see\n[Host-based routing](/en/docs/host-routing).\n\n### Why was I redirected away from `/platform`?\n\nAsking a product-site Host for `/platform` sends you to `PLATFORM_URL`. The console only\nexists on its own Host.\n\n### How do I test tenant subdomains locally?\n\nSet `TENANT_BASE_DOMAIN=localhost` and open `http://{slug}.localhost:7300`. Browsers\nresolve `*.localhost` to the loopback address natively, so there is no hosts file to\nedit.\n\n### My new module\'s page 404s\n\nIts routes must live under `/app/<module>`. Mounted at the top level (say `/site`) they\nget taken over by the CMS on tenant Hosts — `/` belongs to the tenant site, and the app\narea always goes under `/app/*`.\n\n## Database and migrations\n\n### `migrate dev` produced DROP statements I didn\'t ask for\n\nIt diffs your **development database** against the schema, so leftovers in that database\n— old branches, tables from another project — turn into DROPs. Use an offline diff\ninstead: replay the migration history into an empty shadow database and compare that\nagainst the schema, independent of whatever your dev database has accumulated.\n\n### Can I just drop the stray tables in my dev database?\n\n**No.** Dropping tables doesn\'t change the migration history, so you create drift in the\nopposite direction and Prisma escalates from "one extra drop" to "the whole schema must\nbe reset". The only correct fix is making the migration history and the schema agree.\n\n### Where do external modules\' migrations live?\n\nExternal modules only declare `prisma/schema.prisma`; migrations are owned by\n`apps/server`. One database has one migration history — the module\'s schema is symlinked\ninto the main schema and the consumer generates the migration.\n\n### Do I have to run migrations in production?\n\nNot with Docker: `docker/entrypoint.sh` runs `migrate deploy` for you. Outside\ncontainers, run `pnpm --filter server exec prisma migrate deploy` once.\n\n## Building a site\n\n### Why does a new site only have a home page?\n\nThe default marketing starter creates just a home page, on purpose. Add pricing, about\nor contact pages from the page presets, or use the product-site starter.\n\n### Where did the "Get started" header button go?\n\nStarters no longer ship a header button — it used to point at member registration, which\n403s while members are disabled. Add your own call to action in the header settings.\n\n### Can I use `/docs` as a page path?\n\nNo, it is reserved for the documentation library. The same goes for language codes\n(`en`, `zh-CN`).\n\n### I edited a page and visitors still see the old one\n\nThe editor changes the draft; publish it from the list. Pages and documents both work\nthis way.\n\n## Documentation\n\n### How do I write the English version of a document?\n\nChoose the language when creating it — one row per language under the same path. The\npublic site serves the version matching the visitor; the primary language has no URL\nprefix and the rest live at `/{locale}/docs`. See\n[Documentation library](/en/docs/manage-docs).\n\n### Can I change a document\'s language later?\n\nNo. That would move the row into another translation set, which may already contain a\ndocument with the same path. Create it in the target language instead.\n\n### Will "export all" produce duplicate filenames?\n\nNo. The primary language exports as `faq.md` and other languages as `faq.en.md`, and\nimports read the suffix back.\n\n## Deployment\n\n### How do I configure a single-tenant install?\n\nSet `SINGLE_TENANT=true`: the console is hidden, creating tenants is disabled, and every\nrequest belongs to the default tenant. Run `pnpm check:prod-app-env` before committing.\n\n### Do I need to seed the default tenant\'s site?\n\nNo. The server publishes a starter site and documentation library on startup —\nidempotently, and evaluated per language, so it never overwrites content you changed.\n\n## Modules\n\n### `check:modules` is failing\n\nIt verifies registries, tenant columns, toggles, permissions, ordering, shell wiring,\nnavigation and import boundaries. Work through the errors — usually something is missing\nfrom a registry, modules are in the wrong order, or a forbidden package was imported.\n\n### Can an external module depend on another module?\n\nNot by importing it. Cross-module work goes through extension points (events, providers,\nslots). `requires` declares dependency and load order only, not code access.\n',
  },
  {
    locale: "en",
    filename: "getting-started.md",
    raw: "---\ntitle: Getting started\ndescription: Run the platform locally in ten minutes, and learn which address to open\ncategory: getting-started\nsort_order: 0\n---\n\nThree commands to get everything running, then the one thing newcomers always trip\nover: which local address does what.\n\n## Prerequisites\n\n| Tool    | Version   | Used for                        |\n| ------- | --------- | ------------------------------- |\n| Node.js | 22+       | Running the server, building UI |\n| pnpm    | 11+       | Monorepo package management     |\n| Docker  | Any recent | Local PostgreSQL and Redis     |\n\n## 1. Install and initialize\n\n```bash\npnpm install\npnpm setup\n```\n\n`pnpm setup` is **idempotent**: it writes `.env.local`, starts PostgreSQL and Redis in\nDocker, and runs every migration. Re-running it never destroys existing data, so when\nyou are unsure about the current state, just run it again.\n\nDatabase containers only: `pnpm db:up` (and `pnpm db:down` to stop them).\n\n## 2. Start the dev servers\n\n```bash\npnpm dev\n```\n\nOne command starts both server and client. The frontend runs on port 7300, the API on\n3700, and `/api` is proxied across — day to day you only need to remember 7300.\n\n## 3. Which address to open\n\nEverything is routed by **Host**, and this is the part that catches people out:\n\n| Address                              | What it is                                    |\n| ------------------------------------ | --------------------------------------------- |\n| `http://localhost:7300/`             | The product site (the default tenant's site)   |\n| `http://localhost:7300/app`          | The workspace; redirects to `/login` if signed out |\n| `http://localhost:7300/member/login` | Site member sign-in (a separate identity)      |\n| `http://127.0.0.1:7300/platform`     | The platform console                           |\n\n**`localhost` and `127.0.0.1` are two different entrances, not one.** Opening\n`/platform` on `localhost` sends you to `127.0.0.1`; opening `/` on `127.0.0.1` sends\nyou into the console. That split is deliberate — it lets you exercise both Host\nbehaviours locally without editing your hosts file. See\n[Host-based routing](/en/docs/host-routing).\n\n## 4. Open the default tenant's site\n\nThe default tenant (slug `default`) gets its site and this documentation library\n**initialized automatically when the server starts** — no script to run. The starter\nhome page is created and published, and the docs library is seeded per language.\n\nInitialization is **idempotent and evaluated per language**: a language that already\nhas published docs is skipped, so your later edits are never overwritten. To seed the\nsame content into another tenant, or reset the default tenant back to factory content:\n\n```bash\npnpm --filter server exec tsx scripts/seed-local-marketing-site.ts [tenant-slug]\n```\n\n> That script **overwrites** the target tenant's starter pages and doc drafts and\n> republishes them. Think before pointing it at a site that has real content.\n\n## Next\n\n- Understand the entrances → [Host-based routing](/en/docs/host-routing)\n- Understand the architecture → [Multi-tenancy](/en/docs/multi-tenant)\n- Start editing your site → [Building a site](/en/docs/build-site)\n- Ship it → [Installation and deployment](/en/docs/installation)\n",
  },
  {
    locale: "en",
    filename: "host-routing.md",
    raw: "---\ntitle: Host-based routing\ndescription: One process, split by Host into product site, console and tenant sites\ncategory: core-concepts\nsort_order: 20\n---\n\nA single process routes by **Host**, not by URL path. Local and production work the\nsame way, only the domains differ. Once this clicks, most \"why is this a 404\" and \"why\nwas I redirected\" questions answer themselves.\n\n## Three kinds of entrance\n\n| Host                                            | What it is             | What lives there                          |\n| ----------------------------------------------- | ---------------------- | ----------------------------------------- |\n| `FRONTEND_URL` (locally `localhost`)            | Product site = default tenant | `/` site, `/app` workspace, `/member/*` |\n| `PLATFORM_URL` (locally `127.0.0.1`)            | Platform console       | `/platform` (redirects to `/login`)       |\n| `custom_domain` / `{slug}.{TENANT_BASE_DOMAIN}` | Tenant site            | The same three areas                      |\n\nThere are only two redirect rules:\n\n- `/platform` on a tenant Host → sent to `PLATFORM_URL`\n- `/` on the console Host → sent to `/platform`\n\n## Why not split by path\n\nSplitting by path (`/platform/*` is the console, everything else is the site) means\n**every tenant's custom domain carries a console entrance** — their visitors can see an\nadmin area that isn't theirs. Splitting by Host keeps the console on its own Host and\nleaves tenant sites completely clean.\n\n## The app area always lives under `/app/*`\n\nOn a tenant Host, `/` belongs to the tenant's CMS: home page, their own pages, the docs\nlibrary. Everything behind sign-in gets out of the way behind one prefix:\n\n- `/app/dashboard` — workspace\n- `/app/site` — site management\n- `/app/notes` — notes module\n- …\n\nThis is more than a naming habit. That prefix list exists in **three separate places**:\nthe SSR fallback, the nginx location block, and the Vite dev proxy. When every module\nclaimed its own top-level path, all three had to grow together — `/site`,\n`/dashboard` and `/audit-logs` were missed exactly that way and returned 404 on custom\ndomains for a long time. With everything under `/app/*`, all three need just `app`, and\ntop-level slugs go back to the tenant.\n\n## Testing multiple tenants locally\n\nSet the base domain to `localhost`:\n\n```bash\nTENANT_BASE_DOMAIN=localhost\n```\n\nThen open `http://{slug}.localhost:7300`. Browsers resolve `*.localhost` to the\nloopback address natively — **no hosts file editing**. The three entrances stay\nindependent: `localhost` is still the default tenant, `127.0.0.1` is still the console,\n`{slug}.localhost` is that tenant.\n\nTo exercise the custom-domain branch, set some tenant's `custom_domain` to something\nlike `shop.localhost` and open it directly.\n\n## Language prefixes\n\nThe site's primary language has **no** URL prefix — it is the canonical entrance for\nsearch engines. Other languages live under `/{locale}/…`, for example `/en/docs`. A\nlanguage segment and a page path occupy the same position in the URL and are told apart\nby value, which is why `en` and `zh-CN` cannot be used as page paths.\n\n## Next\n\n- Tenants and data isolation → [Multi-tenancy](/en/docs/multi-tenant)\n- Production domains → [Installation and deployment](/en/docs/installation)\n",
  },
  {
    locale: "en",
    filename: "install-external-module.md",
    raw: '---\ntitle: Installing an external module\ndescription: Layout, boundary rules and the wiring steps for external modules\ncategory: core-concepts\nsort_order: 45\n---\n\nAn external module is a self-contained workspace package holding both ends of a\nfeature. It lives under `modules/` at the repo root and talks to the kernel through the\n`@be-water/module-sdk` facade.\n\n## Layout\n\n```\nmodules/\n└── my-module/\n    ├── package.json          # declares the beWater field\n    ├── tsconfig.json\n    ├── MODULE.md             # what this module is for\n    ├── shared/               # cross-end contracts\n    ├── server/               # module.ts + routes + logic\n    ├── client/               # module.tsx + pages + i18n\n    └── prisma/               # optional: schema.prisma\n```\n\n## package.json\n\n```json\n{\n  "name": "@be-water/my-module",\n  "version": "0.0.0",\n  "private": true,\n  "type": "module",\n  "beWater": {\n    "moduleId": "my-module",\n    "prismaSchema": "./prisma/schema.prisma",\n    "requires": ["rbac", "audit"]\n  }\n}\n```\n\n| Field          | Type     | Meaning                                                    |\n| -------------- | -------- | ---------------------------------------------------------- |\n| `moduleId`     | string   | Unique id used by registries, permission prefixes, audit log |\n| `prismaSchema` | string?  | Schema path; when present it is linked into the main schema |\n| `requires`     | string[] | Module ids this one needs — order and prerequisites only, not code access |\n\n## Boundary rules\n\nImports are enforced by `verify-module.mjs`.\n\n**Allowed:**\n\n- `@be-water/module-sdk` — shared contracts\n- `@be-water/module-sdk/server` — server contracts and runtime (server side only)\n- `@be-water/module-sdk/client` — client contracts and runtime (client side only)\n- `@be-water/ui` — UI primitives\n- Third-party libraries (react, react-router, lucide-react, …)\n\n**Forbidden:**\n\n- `@be-water/server-kernel` / `@be-water/client-kit` / `@be-water/shared` — always go\n  through module-sdk; the kernel\'s internals should not become part of a module\'s\n  dependency surface\n- Other modules\' packages — cross-module work goes through extension points\n- Anything under `apps/*`\n\n## Wiring it in\n\n```bash\n# 1. Put the package in modules/<moduleId>/\n\n# 2. Install and generate registries\npnpm install\npnpm gen:external-modules\n\n# 3. The generator may have added workspace deps to apps — install again\npnpm install\n\n# 4. With a Prisma schema: generate the client and a migration\npnpm --filter server exec prisma generate\npnpm --filter server exec prisma migrate dev --name <module_name>\n\n# 5. Verify\npnpm check:modules\npnpm check:deps\npnpm typecheck\n```\n\n### What gen:external-modules does\n\n1. Discovers module packages under `modules/*`\n2. Generates both `apps/{server,client}/src/external-modules.ts` registries\n3. Symlinks the module\'s Prisma schema into `apps/server/prisma/models/<id>.prisma`\n4. Registers tenant columns and the module manifest\n5. Adds `workspace:*` dependencies to `apps/{server,client}/package.json`\n\n## Prisma schema\n\nAn external module only declares models; migrations are generated and owned by\n`apps/server`. **One database has exactly one migration history** — let each module\nown a slice of it and deployment ordering immediately becomes something nobody can\nreason about.\n\n```prisma\nmodel MyEntity {\n  id         String   @id @default(uuid())\n  tenant_id  String\n  name       String\n  created_at DateTime @default(now())\n\n  @@index([tenant_id])\n}\n```\n\n`tenant_id` is not optional: tables without it are rejected by the module contract\nchecks.\n\n## Next\n\n- The bigger picture → [Modular architecture](/en/docs/modular-architecture)\n',
  },
  {
    locale: "en",
    filename: "installation.md",
    raw: '---\ntitle: Installation and deployment\ndescription: Builds, environment variables, Docker images and single-tenant mode\ncategory: getting-started\nsort_order: 10\n---\n\nThe full path from build to production, plus the handful of settings that are easiest\nto get wrong.\n\n## Build and run\n\n```bash\npnpm build\npnpm start\n```\n\n`build` compiles both ends in parallel: Vite emits static assets for the client,\nesbuild bundles the server into a single `apps/server/dist/index.js`. `start` is just\n`node dist/index.js`.\n\n> The server is a **single-file bundle** and does not read sidecar files from the\n> source tree at runtime. Anything that ships with the code — site CSS, the default\n> tenant\'s documentation — is inlined at build time by an `assemble` script. Follow\n> that pattern for new assets of this kind; do not `readFile` a relative path at\n> runtime.\n\n## Environment variables\n\nThe full list lives in `.env.example` at the repo root. The ones you must think about\nbefore going live:\n\n| Variable                       | What it does                                        |\n| ------------------------------ | --------------------------------------------------- |\n| `DATABASE_URL`                 | PostgreSQL connection string                        |\n| `JWT_SECRET`                   | Random string, at least 32 characters               |\n| `TENANT_SECRET_ENCRYPTION_KEY` | Master key for tenant secrets, 32-byte hex (`openssl rand -hex 32`) |\n| `FRONTEND_URL`                 | Product site address                                |\n| `PLATFORM_URL`                 | Console address — **must be a different Host** from `FRONTEND_URL` |\n| `TENANT_BASE_DOMAIN`           | Base domain for tenant subdomains (e.g. `example.com`) |\n| `SINGLE_TENANT`                | `true` enables single-tenant mode                   |\n\n### Why those two URLs need different Hosts\n\nRouting is by Host, not by path. Put them on the same Host and the console and the\nproduct site fight over the same entrance — what you see is `/platform` redirecting\nforever. The reverse proxy depends on the same split: the platform Host serves the\nstatic SPA, every other Host has its HTML proxied to the marketing SSR.\n\n## Docker\n\nThe image is defined in `docker/Dockerfile` in three stages: `builder` installs and\nbuilds, `app` is the Node API process, `web` is Nginx with the static assets.\n\n```bash\n# Exercise the full production image locally\npnpm docker:stack:up\npnpm docker:stack:logs\npnpm docker:stack:down\n```\n\n`docker/entrypoint.sh` detects the migration baseline and runs\n`prisma migrate deploy` before boot, so **a fresh environment needs no manual\nmigration step**. Outside containers, run it once yourself:\n\n```bash\npnpm --filter server exec prisma migrate deploy\n```\n\n## What the first boot does for you\n\nOn startup the server runs an idempotent initialization: create the default tenant,\ngive it a published starter site and documentation library, create the platform system\nadministrator. An empty database is therefore browsable the moment deployment\nfinishes — there is no separate seeding step.\n\n## Single-tenant deployment\n\nFor installations that serve exactly one customer:\n\n```bash\nSINGLE_TENANT=true\n```\n\nThe Tenant model stays, but creating tenants is disabled, self-registration and first\nOAuth sign-in both join the default tenant, tenant-management entrances are hidden,\nand tenant-facing copy never says "tenant".\n\n**Gate:** after changing production env passthrough, run `pnpm check:prod-app-env`.\n\n## Reverse proxy\n\nTenant sites arrive over subdomains (`{slug}.{TENANT_BASE_DOMAIN}`) or custom domains.\nThe proxy must forward **all** of those Hosts to the same backend process and preserve\nthe original `Host` header — that header is how the tenant is resolved.\n\nSee `docker/nginx/default.conf.template` for a working configuration.\n\n## Next\n\n- How the entrances split → [Host-based routing](/en/docs/host-routing)\n- Tenants and domains → [Tenant administration](/en/docs/tenant-management)\n',
  },
  {
    locale: "en",
    filename: "manage-docs.md",
    raw: '---\ntitle: Documentation library\ndescription: Creating, publishing and translating docs, plus import and export\ncategory: build-operate\nsort_order: 70\n---\n\nEvery site has a documentation library, published at `/docs` (index) and\n`/docs/…` (article). The page you are reading lives in one.\n\n## What a document is\n\nA title, a Markdown body and a category. It does not go through the page layout system:\n\n| Field       | Meaning                                              |\n| ----------- | ---------------------------------------------------- |\n| Path        | URL segment (`/docs/<path>`)                         |\n| Language    | One row per translation; with the path it identifies the document |\n| Title       | Title                                                |\n| Summary     | Used in SEO and listings                             |\n| Body        | Markdown body                                        |\n| Category    | The index groups by it                               |\n| Sort weight | Ascending                                            |\n\n**Layout** belongs to the two documentation template pages — writing and layout are\nseparate jobs.\n\n## Drafts and publishing\n\nThe same model as pages:\n\n- The editor changes a **draft**\n- Publishing copies it to the **live** copy that visitors see\n- Draft changes can be reverted back to the live content\n- Unpublishing removes a document from `/docs` while keeping its content\n\nThe workflow is: edit, preview, publish.\n\n## Creating and editing\n\nSite management → Documentation:\n\n1. Click the floating button to create\n2. Fill in title, path, language, category and body\n3. Save (this writes the draft)\n4. Publish from the row actions\n\nThe editor has a full-screen split preview: Markdown on the left, live rendering on the\nright. `⌘S` / `Ctrl+S` saves.\n\n## Path rules\n\n- One segment: starts and ends alphanumeric, hyphens allowed in between\n- Lowercase, at most 63 characters\n- Examples: `quickstart`, `api-reference`, `faq`\n\nThe path is locked once created — changing it breaks every inbound link and search\nresult. To change a path, create a new document and unpublish the old one.\n\n## Languages\n\nEach language is **its own row** under the same path, with independent draft and\npublished state — the path is the translation-group key (same idea as pages: same\npath = translations of each other). Pick the language when creating; it cannot be\nchanged afterwards, because that would move the row into another translation set that\nmay already have a document with the same path.\n\nTo add a translation of an existing document, use **Duplicate to another language** in\nthe row menu. The path stays fixed, the body is copied as a starting point, and the\ncopy is created as a draft — translate it, then publish.\n\nThe public site picks the version matching the visitor:\n\n- The primary language has no URL prefix; others live at `/{locale}/docs`\n- If the requested language has **no documents at all**, the whole library falls back to\n  the primary language instead of showing an empty index or a 404\n- If one document is missing that language, only that document falls back\n- The header language switcher (`show_locale_switcher` in the header settings) lists\n  only **published** translations of the same path. With the switch on but no second\n  published locale, the switcher stays hidden.\n\nOnce the library holds more than one language, the admin list gains a Language column\nand a language filter. With a single language, neither is shown.\n\n## Categories and ordering\n\n- A category is a **site-level entity**: each locale has its own display name, but all\n  languages share the same **category key**. Every translation of a document should pick\n  the same category; visitors see the label for their language.\n- Use **Manage categories** in the document editor to create keys and fill in labels per\n  locale; pick a category from the dropdown when editing a document.\n- The index groups by category; **group order** follows `MarketingDocCategory.sort_order`.\n- Within a category, documents sort by sort weight, then by title.\n- An empty category means **uncategorized**: those documents are listed at the top\n  level, always last, and are never swept into a group called "Other". Don\'t invent\n  categories just to fill the field — a category with one document has a heading that\n  separates nothing.\n- If every document shares one category, neither the sidebar nor the header dropdown\n  draws category headings.\n\nIn import/export frontmatter, `category` is the **category key** (e.g. `getting-started`)\nand must already exist under **Manage categories**.\n\n## Import and export\n\nDocuments move in and out as `.md` files. The filename is the path and frontmatter\ncarries the metadata (field names follow the import format):\n\n```markdown\n---\ntitle: Document title\nslug: quickstart\nlocale: en\ndescription: One-line summary\ncategory: getting-started\nsort_order: 10\n---\n\nBody goes here…\n```\n\n- **Import**: an existing path in the same language has its draft overwritten — updating\n  content is the point of importing. Published documents need publishing again\n  afterwards. When frontmatter **omits** `sort_order`, the existing order is left alone;\n  ordering is usually something you dragged into place and shouldn\'t be reset by an\n  import.\n- **Export**: one document or all of them, exporting draft content exactly as the editor\n  shows it.\n\nThe language travels with the file: frontmatter `locale` first, then a filename suffix\n(`faq.en.md`), and failing both it lands in the site\'s primary language. On export the\nprimary language is `faq.md` and other languages are `faq.en.md` — so "export all"\nnever produces duplicate filenames, and an export can be re-imported as-is.\n\n## Next\n\n- Change the document layout → [Pages and layout](/en/docs/manage-pages)\n- Put docs in the navigation → [Building a site](/en/docs/build-site)\n',
  },
  {
    locale: "en",
    filename: "manage-pages.md",
    raw: '---\ntitle: Pages and layout\ndescription: The section system, columns, drafts and page presets\ncategory: build-operate\nsort_order: 60\n---\n\nEvery page is a sequence of sections. This is how to arrange them.\n\n## The sections\n\n| Section        | Name         | What it does                        |\n| -------------- | ------------ | ----------------------------------- |\n| `header`       | Header       | Site-level, shared by every page    |\n| `footer`       | Footer       | Site-level, shared by every page    |\n| `hero`         | Hero         | Opening statement plus main button  |\n| `page-header`  | Page title   | Title block for inner pages         |\n| `feature-grid` | Feature grid | Grid of selling points with icons   |\n| `cards`        | Cards        | A group of content cards            |\n| `steps`        | Steps        | An ordered process                  |\n| `spec-list`    | Spec list    | Key/value specifications            |\n| `pricing`      | Pricing      | Pricing table                       |\n| `faq`          | FAQ          | Collapsible questions               |\n| `band`         | CTA band     | One full-width call to action       |\n| `prose`        | Rich text    | Write Markdown directly             |\n| `form`         | Form         | Collect leads; submissions are stored |\n| `page-menu`    | Page menu    | Page-level table of contents        |\n| `group`        | Columns      | A container that puts sections side by side |\n| `doc-list`     | Doc list     | Documentation index                 |\n| `doc-nav`      | Doc navigation | Sidebar list of documents         |\n| `doc-toc`      | On this page | Headings of the current document    |\n| `doc-article`  | Doc body     | The document body itself            |\n\nThe four documentation sections only make sense on the documentation template pages —\nthey read from the docs library.\n\nDrag to reorder, add and remove, and edit each section\'s settings in the page editor.\n\n## Columns\n\nTo place two blocks side by side, add a Columns section and put sections into each\ncolumn.\n\n- **Column count**: add or remove columns in the structure tree on the left, up to 4\n- **Widths**: select the Columns section and drag the handles on the width control. A\n  row is 12 units and you drag the boundary *between* columns, so the widths always add\n  up to a full row — you can never end up half configured with a gap left over\n- **Dividers**: select a column and tick "divider on the right" to draw a vertical rule\n  between it and the next column, full row height. The last column has none — there is\n  nothing to its right to separate. Style, thickness and colour are per column, so one\n  section can carry different-looking rules; leave the colour empty to follow the\n  theme\'s border colour\n\nNarrow screens always stack vertically and ignore the widths; the per-column "stack\norder" decides what comes first.\n\n## Drafts and publishing\n\nEach page holds two copies:\n\n- The **draft**, which the editor is changing and visitors cannot see\n- The **live** copy, which is what visitors get\n\nPublishing copies the draft over the live copy; reverting copies it back. The "unpublished\nchanges" marker in the list simply means the two differ.\n\nDocuments work exactly the same way: edit the draft, check it, then publish. Work in\nprogress is never exposed. Published pages also keep version history, so you can see who\nchanged what and when.\n\n## Page presets\n\nWhen creating a page you can start from a preset — a ready-made arrangement of sections:\n\n- Home (default layout)\n- Pricing\n- About\n- Contact\n\nThere are also two **documentation template pages** (docs index and docs article). They\ndon\'t appear in the page list; you only create them if you want to change how documents\nare laid out. Until then the public site renders them with the built-in layout.\n\n## Page paths\n\nA page\'s path comes from its path field: the home page is always `/`; other pages map\ndirectly, for example `pricing` → `/pricing`.\n\n> `/docs` is reserved for the documentation library and cannot be used as a page path.\n> The same applies to language codes (`en`, `zh-CN`) — they occupy the same position in\n> the URL.\n\n## Page order\n\nThe order in the page list is the order visitors see: the header\'s "all pages" nav, page\nmenus and the sitemap all follow it. Use the move up / move down buttons on each row —\nthe change takes effect immediately, with nothing to publish.\n\nEvery language version of a page counts as **one group** and moves together; in the nav\nthey occupy the same position anyway.\n\n> The move buttons disappear while the list is filtered: what you see then is not the\n> real order, so moving rows would be moving blind. Clear the filters first.\n\n## Status and visibility\n\n- **Status**: draft (invisible to visitors) or published. New pages start as drafts.\n- **Visibility**: visible to everyone, or members only. With members-only, a signed-out\n  visitor receives a page skeleton with no body at all; the body arrives after sign-in.\n  It is not hidden client-side.\n\n## SEO\n\nA page\'s title and description become its SEO metadata. For detail pages (documents),\nSEO **follows the content itself** rather than the template page — every document has\nits own title and summary, and reusing the template\'s would give you dozens of identical\nsearch results.\n\n## Next\n\n- Manage documentation → [Documentation library](/en/docs/manage-docs)\n- Gate content behind sign-in → [Site members](/en/docs/members)\n',
  },
  {
    locale: "en",
    filename: "members.md",
    raw: "---\ntitle: Site members\ndescription: Member accounts, sign-in options and members-only content\ncategory: build-operate\nsort_order: 80\n---\n\nMembers are the account system for your site's **visitors**. They are entirely separate\nfrom the users who run the site from the workspace.\n\n## Members vs users\n\n|              | Member                    | User                   |\n| ------------ | ------------------------- | ---------------------- |\n| Who          | A site visitor, your customer | Someone who runs the site |\n| Entrance     | `/member/*`               | `/app/*`               |\n| Sign-in page | `/member/login`           | `/login`               |\n| Can do       | Read members-only content | Manage the site and its data |\n| Managed in   | Site management → Members | Admin → Users          |\n\nThe two sessions are independent: one person can be a member of your site and an\nadministrator elsewhere without conflict.\n\n## Enabling members\n\nMembers are a per-tenant capability and are **off by default**. While off, no sign-in or\naccount entrance appears in the header — a button that leads to a 403 is worse than no\nbutton.\n\nOnce enabled the header gains a member entrance whose appearance you control in the\nheader settings.\n\n## Sign-in options\n\nMembers sign in at `/member/login`, with credentials configured per tenant: email and\npassword, or OAuth (GitHub, Google, Microsoft and others).\n\nOAuth needs application credentials configured on the platform side. The workspace and\nsite members **share one IdP redirect URI**; the callback routes to the right identity\nsystem by state — you don't need to register two OAuth applications.\n\n## Members-only content\n\nSet a page's visibility to \"members only\":\n\n- Signed-out visitors receive a page skeleton **with no body**; the content is never\n  sent\n- Signed-in members have the body filled in\n- Search engines only index the public part\n\nThe point is that the server withholds it, rather than the browser hiding it. Paid\ncontent hidden with CSS is two clicks away from being read.\n\n## Managing members\n\nSite management → Members: browse the list, enable or disable accounts, review activity.\n\nMembers manage their own profile and password at `/member/account`.\n\n## When you need members\n\nSites that gate content: knowledge bases, courses, member-only downloads, content\nunlocked after a visitor leaves their details.\n\nPurely promotional sites — landing pages, brochure sites — don't need any of it. Leave\nit off.\n\n## Next\n\n- Set page visibility → [Pages and layout](/en/docs/manage-pages)\n- Workspace users and permissions → [Users and permissions](/en/docs/users-permissions)\n",
  },
  {
    locale: "en",
    filename: "modular-architecture.md",
    raw: "---\ntitle: Modular architecture\ndescription: Built-in modules, external modules, registries and contract checks\ncategory: core-concepts\nsort_order: 40\n---\n\nFunctionality is organized into **modules** that can be switched on per deployment.\n`apps/server` and `apps/client` are thin assembly layers; the logic lives in module\npackages.\n\n## What a module contains\n\n| Part      | Holds                                                |\n| --------- | ---------------------------------------------------- |\n| `shared/` | Cross-end contracts: types, validation, pure logic    |\n| `server/` | Routes, business logic, that module's Prisma schema   |\n| `client/` | Pages, routes, translations                           |\n\nEach module implements `ServerAppModule` / `ClientAppModule` in `module.ts` /\n`module.tsx`, registering its routes, navigation, permissions and copy with the kernel.\n\n## Built-in modules\n\nUnder `packages/builtin/*`, shipped with the platform:\n\n| Module                     | Responsibility                              |\n| -------------------------- | ------------------------------------------- |\n| `user`                     | Users and JWT authentication                |\n| `rbac`                     | Roles and permissions                       |\n| `platform`                 | Platform console: tenants, plans, quotas    |\n| `marketing`                | Site CMS: page layout, docs library, member entrances |\n| `site-member`              | Site member identity                        |\n| `billing`                  | Subscriptions and payments                  |\n| `audit`                    | Audit log                                   |\n| `notification`             | In-app notifications                        |\n| `background-job`           | Job centre                                  |\n| `error-log` / `slow-query` | Observability                               |\n| `dashboard`               | Workspace card aggregation                  |\n\n## External modules\n\nThird-party and bespoke features live in `modules/*` at the repo root (the shipped\nexamples are `note`, `todo` and `bookmark`):\n\n- They talk to the kernel only through the `@be-water/module-sdk` facade\n- Import boundaries are enforced by `verify-module.mjs`\n- They bring their own Prisma schema; migrations stay with `apps/server`\n\nSee [Installing an external module](/en/docs/install-external-module).\n\n## Modules never import each other\n\nA module cannot `import` another module's code. Cross-module work goes through kernel\nextension points: the event bus, providers, slots. `requires` declares dependency and\nload order only — it does not grant code-level access.\n\nThe reason is one-directional layering: the kernel and infrastructure don't know about\nbusiness modules, business modules don't know about each other, and any one of them can\nbe deleted without disturbing the rest.\n\n## Registries\n\nTwo registries decide what is enabled:\n\n| Registry              | Path                                 |\n| --------------------- | ------------------------------------ |\n| Server enabled modules | `apps/server/src/enabled-modules.ts` |\n| Client enabled modules | `apps/client/src/enabled-modules.ts` |\n\nOrder is `infrastructure → shell → business`, so dependencies load before their users.\nThe external-module section is generated by `pnpm gen:external-modules` — don't hand\nedit it.\n\n## The route prefix is a hard rule\n\nA new module's `renderRoutes` / `nav.path` / `mobileTabPaths` **must** use the\n`/app/<module>` prefix (`/app/site`, `/app/notes`). Routes mounted at the top level get\nswallowed by the CMS on tenant domains. The reasoning is in\n[Host-based routing](/en/docs/host-routing).\n\n## Generating and checking\n\n```bash\npnpm gen:module <spec.yaml>          # scaffold a module from a spec\npnpm check:modules                   # registries, tenant columns, toggles, permissions, order, shell, nav, import boundaries\npnpm check:deps                      # circular dependency detection\nnode scripts/verify-module.mjs <id>  # check a single module\n```\n\nThe standard path is: write the spec → `gen:module` → fill in the logic →\n`check:modules`.\n",
  },
  {
    locale: "en",
    filename: "multi-tenant.md",
    raw: '---\ntitle: Multi-tenancy\ndescription: Tenant isolation, data scoping, the default tenant and single-tenant mode\ncategory: core-concepts\nsort_order: 30\n---\n\nThe platform is multi-tenant by design: one deployment serves many tenants that never\nsee each other, each with its own site, users and data.\n\n## What a tenant is\n\nA tenant is the top-level isolation boundary. One tenant is one organization or\ncustomer, and owns:\n\n- Its marketing site (pages, docs, theme)\n- Its users and their roles\n- Its site members\n- Its business data, scoped by `tenant_id`\n\n> In tenant-facing and public copy the word "tenant" never appears — to an end user it\n> is simply "your site" or "your organization". This is not fussiness about wording:\n> the same code has to ship as a single-customer private deployment, and there "tenant"\n> is an internal concept the customer should never meet.\n\n## Data isolation\n\nEvery business table carries a `tenant_id`, and queries go through\n`withTenantScope(tenant_id)`:\n\n```typescript\nconst records = await prisma.marketingDoc.findMany({\n  where: withTenantScope(tenant_id, { status: "published" }),\n});\n```\n\nIsolation is guaranteed by that **single entry point**, not by every call site\nremembering to filter. Hand-written `where` clauses that bypass it are rejected by the\nmodule contract checks.\n\n## The default tenant\n\nThe tenant with slug `default` is implicitly bound to the product domain. What you see\nat `FRONTEND_URL` (locally `localhost`) is its site.\n\nIts site and documentation library are **initialized when the server starts**,\nidempotently and per language: a language that already has published docs is skipped,\nso nothing you edited gets overwritten. Deploying an upgrade will not clobber your\nhome page, and the starter content for a newly added language appears on the next\nboot.\n\n## Creating and managing tenants\n\nNew tenants are created in the platform console (`PLATFORM_URL`, locally\n`127.0.0.1`). A new tenant is active but its **site is empty** — automatic\ninitialization only applies to the default tenant. Two ways to give it content: the\ntenant\'s own administrator applies a starter from the workspace, or a platform\nadministrator does it for them.\n\n## Single-tenant mode\n\nFor private deployments, set `SINGLE_TENANT=true`:\n\n- The console and tenant management are hidden\n- Creating tenants is disabled; self-registration and first OAuth sign-in join the\n  default tenant\n- Nothing in the UI exposes the multi-tenant concept\n\nSingle-tenant and multi-tenant **share the same code**; the difference is runtime\nconfiguration. There is no separate branch to maintain for private installs.\n\n## Routing follows the Host\n\n- `FRONTEND_URL` → default tenant\n- `{slug}.{TENANT_BASE_DOMAIN}` → the tenant with that slug\n- A tenant\'s custom domain → that tenant\n\nSee [Host-based routing](/en/docs/host-routing).\n',
  },
  {
    locale: "en",
    filename: "tenant-management.md",
    raw: "---\ntitle: Tenant administration\ndescription: Tenants, quotas, entitlements and domains in the platform console\ncategory: platform-admin\nsort_order: 100\n---\n\nTenants are managed in the **platform console**, not from a tenant's workspace. Only\nplatform administrators can reach it.\n\n## Getting in\n\nLocally that is `http://127.0.0.1:7300/platform`; signed-out visits redirect to\n`/login`.\n\n> It has to be `127.0.0.1`, not `localhost` — the latter is the product site entrance,\n> and asking it for `/platform` bounces you back to the console Host. See\n> [Host-based routing](/en/docs/host-routing).\n\nIn production this is whatever Host `PLATFORM_URL` points at.\n\n## The tenant list\n\nThe console lists every tenant with its status, creation date, usage figures and\nenabled modules. You can filter by status and search by name.\n\n## Creating a tenant\n\nYou provide:\n\n- **slug** — used for the subdomain `{slug}.{TENANT_BASE_DOMAIN}`, so it becomes part of\n  a public address\n- **name** — for display\n- **default language** — the site's primary language, which decides whose URLs go\n  without a prefix\n\nA new tenant is active but its **site is empty**; automatic content only happens for the\ndefault tenant. Either the tenant's administrator applies a starter from the workspace,\nor a platform administrator does it for them.\n\n## Quotas\n\nEach tenant has a set of limits: users, pages, documents, storage and so on. Quotas come\nfrom plan tiers and can be adjusted for an individual tenant.\n\n## Entitlements\n\nFeatures are controlled by entitlements: which modules are available, whether members\ncan be enabled, whether custom domains can be bound, and so on. They are tied to the\nbilling plan — upgrading unlocks more.\n\n## Custom domains\n\n1. The tenant points their domain at the platform with a CNAME\n2. A platform administrator registers that domain against the tenant\n3. The platform verifies ownership, and it goes live\n\nOnce bound, the domain serves that tenant's site and behaves exactly like a subdomain.\n\n## Suspending a tenant\n\nSuspension hides the site from visitors and blocks workspace sign-in, while **keeping\nall data**. It is fully reversible.\n\nDeleting is a separate, irreversible operation. For routine cleanup, prefer suspension.\n\n## Audit\n\nEvery tenant administration action — creating, suspending, changing quotas, binding\ndomains — is written to the audit log, filterable by module, action and time in the\nconsole.\n\n## Next\n\n- Users inside a tenant → [Users and permissions](/en/docs/users-permissions)\n- Private single-tenant installs → [Installation and deployment](/en/docs/installation)\n",
  },
  {
    locale: "en",
    filename: "users-permissions.md",
    raw: '---\ntitle: Users and permissions\ndescription: User management, roles and the RBAC permission model\ncategory: platform-admin\nsort_order: 110\n---\n\nThis covers users inside a tenant. "User" here means someone who works in the\nworkspace — not a site member; see [Site members](/en/docs/members) for the difference.\n\n## Managing users\n\nAdmin → Users:\n\n- Create a user (username plus an initial password)\n- Edit details, enable or disable, reset the password\n- Assign roles\n\nUsers sign in to `/app/*`; which modules they see depends on their permissions.\n\n## Where permissions come from\n\nThere are exactly two sources:\n\n1. **The system administrator flag.** A user with it holds every permission in the\n   tenant and needs no roles at all. Keep at least one such user, or nobody can grant\n   anything to anyone.\n2. **Roles.** For everyone else, permissions come **only** from assigned roles. No\n   roles means no permissions.\n\nNew tenants ship with no predefined roles. A preset "Editor" means something different\nat every organization, so rather than guess, the first administrator builds the roles\nthat match how the team actually works.\n\n## Permission names\n\nPermissions are named `<module>.<action>` and each module declares its own, which are\nmerged into a catalogue:\n\n| Permission    | Meaning                |\n| ------------- | ---------------------- |\n| `site.read`   | View site content      |\n| `site.write`  | Edit site content      |\n| `user.manage` | Manage users           |\n| `role.manage` | Manage roles           |\n| `audit.read`  | Read the audit log     |\n\nOnly keys that exist in the catalogue take effect. Typing one by hand raises no error\nbut never matches anything, so build roles by ticking entries from the catalogue.\n\n## Managing roles\n\nAdmin → Roles: browse the permission catalogue, create a role and tick its permissions,\nedit it, or delete it (roles still assigned must be unassigned first).\n\n## Checking permissions\n\nOn the client:\n\n```tsx\nconst { hasPermission } = usePermissions();\nif (hasPermission("site.write")) {\n  // show the edit button\n}\n```\n\nEntrances the user lacks permission for are **not rendered at all**, rather than\ndisabled — a button that does nothing just generates questions.\n\nOn the server:\n\n```typescript\napp.get("/api/site/docs", { preHandler: [app.requirePermission("site.read")] }, handler);\n```\n\nThe server check is the only one that actually enforces anything. Hiding UI is a\ncourtesy; anyone can call the endpoint directly.\n\n## Audit log\n\nEvery write is audited: who, when, which resource, what changed. Filter by module,\naction and time in Admin → Audit log.\n\nNew modules must audit their writes too — it is one of the module contract checks, and\n`check:modules` fails when it is missing.\n\n## Members, for comparison\n\n|            | User             | Member                    |\n| ---------- | ---------------- | ------------------------- |\n| Who        | Runs the site    | Visits the site           |\n| Entrance   | `/app/*`         | `/member/*`               |\n| Permissions | RBAC roles      | Signed in as a member, or not |\n| Managed in | Admin → Users    | Site management → Members |\n',
  },
  {
    locale: "zh-CN",
    filename: "build-site.md",
    raw: "---\ntitle: 建站与主题\ndescription: 起步模板、主题、页头页脚与导航\ncategory: build-operate\nsort_order: 50\n---\n\n每个站点有一个营销站点，由页面、主题、页头页脚组成。这一篇讲从起步模板到站点上线的\n那条主路径。\n\n## 起步模板\n\n新站点从起步模板开始。一个模板 = 一个主题包 + 一组页面：\n\n| 模板     | 适用场景 | 建出哪些页面                  |\n| -------- | -------- | ----------------------------- |\n| 默认营销站 | 通用起步 | 首页                          |\n| 产品官网 | 产品官网 | 首页 + 定价 + 关于 + 联系     |\n| 单页落地 | 单页落地 | 首页 + 联系（留白更大的主题） |\n\n在站点管理页一键应用。应用模板会**覆盖同名页面的内容**，但不会删掉你自建的页面。\n\n> 「默认营销站」只建一个首页，是刻意的：起步模板铺出来的每一页，往往第一件事就是删\n> 掉它。要定价页、关于页就在页面预设里加一页，成本比删干净低得多。\n\n## 主题\n\n主题是一组设计 token（颜色、字体、间距、圆角），决定站点的视觉基调：\n\n- 在站点设置里切换主题包\n- 单个 token 可以站点级覆盖（比如只换主色）\n- Logo 是品牌资产，独立于主题单独配置\n\n## 页头与页脚\n\n页头与页脚是**站点级**的段，所有页面共用一份：\n\n- 页头：logo、站名、导航、是否吸顶、布局\n- 页脚：logo、简介、版权、链接组\n\n> 模板**不**预置页头按钮。需要「免费开始」这类 CTA 的，在页头设置里自己加一条——\n> 预置一个指向未开通功能的按钮，比没有按钮更糟。\n\n### 导航链接\n\n选中页头后，右侧的「导航链接」就是访客看到的那一排。点「添加」可选四种来源：\n\n| 来源         | 展开成什么                                       |\n| ------------ | ------------------------------------------------ |\n| 自定义链接   | 一条你自己填的链接，可再挂一层子菜单             |\n| 全部一级页面 | 所有已发布的一级页面，新建页面自动进导航         |\n| 文档库       | 整个文档库，按分类分组（父项链到 `/docs`）       |\n| 某个文档分类 | 指定分类下的全部已发布文档                       |\n\n后三种是**规则**而不是固定条目：展开成哪些链接由内容决定，编辑器里会写着「会显示：\n…」。写着「现在展开不出任何内容」时，这一条在前台不渲染——不会留下一个点进去是空\n列表的入口。\n\n建站默认配的是「全部一级页面」+「文档库」。所以写了第一篇文档之后，页头会自动长出\n「文档」入口；还没写时那一条自动消失。文档索引页本身不算一级页面，不会重复出现。\n\n页脚的每一列有自己的链接列表。想和页头一致时点「从页头复制」——复制的是快照，之后\n两边各改各的。\n\n## 站名与标语\n\n站名与标语显示在页头和 SEO 元信息里，支持按语言分别填写。开站第一件事就是把模板的占位文案改掉。\n\n## 多语言\n\n站点支持多语言，**主语言的 URL 不带前缀**（它是 SEO 主入口），其余语言走\n`/{locale}/…`。页面与文档都是**按语言分行存**的：同一路径每种语言各一份，它们靠路径\n自动成为一组译文，语言切换器和 hreflang 据此生成。\n\n某种语言一篇文档都没有时，文档库整库回落主语言展示——不 404。\n\n## 发布\n\n站点有一个总开关：关着时访客看到占位页，开了才能看到内容。\n\n页面和文档各自还有独立的草稿 / 已发布状态。**总开关只是总开关**，具体内容上线仍要\n逐个发布。\n\n## 下一步\n\n- 编排页面版式 → [页面与版式](/docs/manage-pages)\n- 管理文档内容 → [文档库管理](/docs/manage-docs)\n",
  },
  {
    locale: "zh-CN",
    filename: "faq.md",
    raw: "---\ntitle: 常见问题\ndescription: 本地开发、部署、建站与模块的高频疑问\nsort_order: 120\n---\n\n## 本地开发\n\n### 为什么 `localhost:7300` 和 `127.0.0.1:7300` 看到的不一样？\n\n它们是两个入口。`localhost` 是产品站（默认租户），`127.0.0.1` 是平台控制台。这是\n按 Host 分流的结果，详见 [Host 分流机制](/docs/host-routing)。\n\n### 访问 `/platform` 被重定向了？\n\n在产品站 Host 上访问 `/platform` 会被送去 `PLATFORM_URL`。控制台只在它自己的 Host 上\n开放。\n\n### 租户子域本地怎么测？\n\n设 `TENANT_BASE_DOMAIN=localhost`，然后直接访问 `http://{slug}.localhost:7300`。\n浏览器原生把 `*.localhost` 解析到回环地址，不用改 hosts 文件。\n\n### 新模块的页面 404？\n\n路由必须挂在 `/app/<模块>` 下。挂在顶层（如 `/site`）时，在租户 Host 上会被 CMS\n接管——`/` 归租户站点，应用区一律走 `/app/*`。\n\n## 数据库与迁移\n\n### `migrate dev` 生成了意料之外的 DROP？\n\n它是拿**开发库**和 schema 比对生成迁移的，开发库的历史痕迹（旧分支、别的项目残留的\n表）会变成 DROP。改用离线 diff：把迁移历史重放到一个空的影子库，再和 schema 比，\n与开发库现状无关。\n\n### 能直接删开发库里的表来消除漂移吗？\n\n**不能。** 删表不改迁移历史，等于制造反向漂移，Prisma 会从「多生成一条 drop」升级成\n「必须 reset 整个 schema」。唯一正确的路是让迁移历史与 schema 一致。\n\n### 外部模块的迁移在哪？\n\n外部模块只声明 `prisma/schema.prisma`，迁移统一由 `apps/server` 管理。一个数据库只有\n一条迁移历史——模块的 schema 被符号链接进主 schema，迁移由消费方生成。\n\n### 生产要手动跑迁移吗？\n\nDocker 部署不用，`docker/entrypoint.sh` 会自动 `migrate deploy`。非容器部署时手工跑\n一次 `pnpm --filter server exec prisma migrate deploy`。\n\n## 建站\n\n### 新站点为什么只有一个首页？\n\n「默认营销站」起步模板只建首页，这是刻意的。要定价 / 关于 / 联系页，在页面预设里加，\n或换用「产品官网」模板。\n\n### 页头怎么没有「免费开始」按钮？\n\n模板不预置页头按钮——它以前指向会员注册，而会员默认未开通，点进去是 403。需要 CTA\n就在页头设置里自己加一条。\n\n### `/docs` 能拿来当页面路径吗？\n\n不能，它是文档库的专属路径。语言代码（`en`、`zh-CN`）同理。\n\n### 改完页面访客看不到变化？\n\n编辑器改的是草稿，要在列表里点「发布」才上线。页面和文档都是这个口径。\n\n## 文档库\n\n### 怎么写英文版的文档？\n\n新建文档时选语言，同一路径每种语言各存一行。前台按访客语言取对应版本，主语言不带\nURL 前缀，其余走 `/{locale}/docs`。详见 [文档库管理](/docs/manage-docs)。\n\n### 建好之后能改语言吗？\n\n不能。改语言等于把这一行搬进另一组译文，而那边可能已经有同路径的文档。要换语言就\n在目标语言里新建一篇。\n\n### 导出全部会不会重名？\n\n不会。主语言导出成 `faq.md`，其余语言导出成 `faq.en.md`，导入时能认回去。\n\n## 部署\n\n### 单租户怎么配？\n\n设 `SINGLE_TENANT=true`：隐藏控制台、禁止新建租户、所有请求归默认租户。提交前跑\n`pnpm check:prod-app-env`。\n\n### 默认租户的站点内容需要手工初始化吗？\n\n不需要。服务端启动时自动铺一套已发布的起步站点与文档库，幂等，而且按语言分别判断——\n不会覆盖你改过的内容。\n\n## 模块\n\n### `check:modules` 报错怎么办？\n\n它校验注册表、租户列、开关、权限、排序、外壳、nav 与 import 边界。按报错逐项修\n——通常是注册表漏登记、模块顺序不对，或 import 了不该 import 的包。\n\n### 外部模块能依赖别的模块吗？\n\n不能直接 `import`。跨模块走扩展点（事件、Provider、Slot）。`requires` 只声明依赖\n关系与加载顺序，不给代码级访问。\n",
  },
  {
    locale: "zh-CN",
    filename: "getting-started.md",
    raw: "---\ntitle: 快速入门\ndescription: 十分钟在本地把平台跑起来，并弄清「从哪个地址进」\ncategory: getting-started\nsort_order: 0\n---\n\n三条命令把整套平台跑起来，然后弄清本地那两个地址的区别——后者是新人最常踩的坑。\n\n## 前置依赖\n\n| 依赖    | 版本要求 | 用途                       |\n| ------- | -------- | -------------------------- |\n| Node.js | 22+      | 运行 server 与前端构建     |\n| pnpm    | 11+      | monorepo 包管理            |\n| Docker  | 任意近版 | 本地 PostgreSQL 与 Redis   |\n\n## 1. 装依赖并初始化\n\n```bash\npnpm install\npnpm setup\n```\n\n`pnpm setup` 是**幂等**的一键初始化：写出 `.env.local`、用 Docker 起 PostgreSQL 与\nRedis、跑完所有迁移。重复执行不会破坏已有数据，所以拿不准状态时再跑一遍就好。\n\n只想起数据库容器：`pnpm db:up`（停掉是 `pnpm db:down`）。\n\n## 2. 启动开发服务器\n\n```bash\npnpm dev\n```\n\n一条命令同时起 server 与 client。前端在 7300 端口，API 在 3700，`/api` 由 Vite 代理\n过去——日常开发只需要记住 7300。\n\n## 3. 从哪个地址进\n\n本地按 **Host** 分流，这是最容易踩坑的一点：\n\n| 地址                              | 是什么                                       |\n| --------------------------------- | -------------------------------------------- |\n| `http://localhost:7300/`          | 产品站（默认租户的官网）                     |\n| `http://localhost:7300/app`       | 工作台，未登录自动转 `/login`                |\n| `http://localhost:7300/member/login` | 站点会员登录（与工作台用户是两套身份）    |\n| `http://127.0.0.1:7300/platform`  | 平台控制台                                   |\n\n**`localhost` 与 `127.0.0.1` 是两个入口，不是同一个。** 在 `localhost` 上访问\n`/platform` 会被送去 `127.0.0.1`；在 `127.0.0.1` 上访问 `/` 会被送进控制台。这是刻意\n的设计，让本地不改 hosts 就能同时验证两种 Host。详见 [Host 分流机制](/docs/host-routing)。\n\n## 4. 打开默认租户的站点\n\n默认租户（slug 为 `default`）的站点与这份文档库在**服务端启动时自动初始化**，不需要\n手工执行任何脚本：起步模板的首页会被创建并发布，文档库按语言各铺一套。\n\n初始化是**幂等**的，而且按语言分别判断：某种语言已经有已发布文档就跳过那一份，不会\n覆盖你后来的编辑。想给别的租户铺同样一套内容，或把默认租户重置回出厂内容：\n\n```bash\npnpm --filter server exec tsx scripts/seed-local-marketing-site.ts [租户 slug]\n```\n\n> 这个脚本会**覆盖**目标租户的起步页面与文档草稿并重新发布。对着有内容的站点执行前\n> 先想清楚。\n\n## 下一步\n\n- 理解入口分流 → [Host 分流机制](/docs/host-routing)\n- 了解整体架构 → [多租户架构](/docs/multi-tenant)\n- 动手改站点 → [建站与主题](/docs/build-site)\n- 部署上线 → [安装与部署](/docs/installation)\n",
  },
  {
    locale: "zh-CN",
    filename: "host-routing.md",
    raw: "---\ntitle: Host 分流机制\ndescription: 同一个进程按 Host 分流到产品站、控制台与租户站点\ncategory: core-concepts\nsort_order: 20\n---\n\n同一个进程按 **Host** 分流，不是按 URL 路径。本地与生产同构，只是换成真实域名。理解\n这一条之后，很多「为什么 404」「为什么被重定向」都会豁然开朗。\n\n## 三类入口\n\n| Host                                            | 是什么          | 能访问什么                                |\n| ----------------------------------------------- | --------------- | ----------------------------------------- |\n| `FRONTEND_URL`（本地 `localhost`）              | 产品站=默认租户 | `/` 官网、`/app` 工作台、`/member/*` 会员 |\n| `PLATFORM_URL`（本地 `127.0.0.1`）              | 平台控制台      | `/platform`（未登录转 `/login`）          |\n| `custom_domain` / `{slug}.{TENANT_BASE_DOMAIN}` | 租户站点        | 与产品站相同的三块                        |\n\n分流规则只有两条：\n\n- 租户 Host 上访问 `/platform` → 送去 `PLATFORM_URL`\n- 控制台 Host 上访问 `/` → 送去 `/platform`\n\n## 为什么不按路径分\n\n按路径分（`/platform/*` 归控制台、其余归站点）意味着**每个租户的自定义域名上都挂着\n一个控制台入口**——租户的访客能看到一个不属于他们的管理后台。按 Host 分让控制台只在\n自己的 Host 上存在，租户站点上彻底干净。\n\n## 应用区一律在 `/app/*` 之下\n\n租户 Host 的 `/` 归租户 CMS：首页、自建页面、文档库都在那儿。登录后的应用区靠一级\n路径前缀让开：\n\n- `/app/dashboard` — 工作台\n- `/app/site` — 站点管理\n- `/app/notes` — 笔记模块\n- ……\n\n这不只是命名习惯。这份前缀表在 SSR 兜底、nginx location、Vite dev 代理里**各有一份\n副本**，每个模块各占一个顶层路径时三份都得跟着长——`/site`、`/dashboard`、\n`/audit-logs` 就是这么漏掉的，在绑定域名上一直返回 404。收进 `/app/*` 之后三处都只\n需要一个 `app`，顶层 slug 也还给了租户站点。\n\n## 本地怎么测多租户\n\n把基域设成 `localhost`：\n\n```bash\nTENANT_BASE_DOMAIN=localhost\n```\n\n然后直接访问 `http://{slug}.localhost:7300`。浏览器原生把 `*.localhost` 解析到回环\n地址，**不用改 hosts 文件**。三个入口互不影响：`localhost` 仍是默认租户、\n`127.0.0.1` 仍是平台控制台、`{slug}.localhost` 是对应租户。\n\n要验自定义域名那条分支，把某个租户的 `custom_domain` 设成 `shop.localhost` 之类的值\n即可，同样直接可访问。\n\n## 语言前缀\n\n站点主语言的 URL **不带**语言前缀（它是 SEO 主入口），其余语言走 `/{locale}/…`\n子目录，例如 `/en/docs`。语言段与页面路径在 URL 上是同一个位置，靠取值区分——所以\n`en`、`zh-CN` 这类值不能拿来当页面路径。\n\n## 下一步\n\n- 租户与数据隔离 → [多租户架构](/docs/multi-tenant)\n- 生产域名配置 → [安装与部署](/docs/installation)\n",
  },
  {
    locale: "zh-CN",
    filename: "install-external-module.md",
    raw: '---\ntitle: 安装外部模块\ndescription: 外部模块的目录结构、边界规则与接入流程\ncategory: core-concepts\nsort_order: 45\n---\n\n外部模块是一个自带前后端定义的独立 workspace 包，放在仓库根的 `modules/` 下，通过\n`@be-water/module-sdk` 门面与内核交互。\n\n## 目录结构\n\n```\nmodules/\n└── my-module/\n    ├── package.json          # 声明 beWater 字段\n    ├── tsconfig.json\n    ├── MODULE.md             # 这个模块是干什么的\n    ├── shared/               # 跨端契约\n    ├── server/               # module.ts + 路由 + 业务逻辑\n    ├── client/               # module.tsx + 页面 + i18n\n    └── prisma/               # 可选：schema.prisma\n```\n\n## package.json\n\n```json\n{\n  "name": "@be-water/my-module",\n  "version": "0.0.0",\n  "private": true,\n  "type": "module",\n  "beWater": {\n    "moduleId": "my-module",\n    "prismaSchema": "./prisma/schema.prisma",\n    "requires": ["rbac", "audit"]\n  }\n}\n```\n\n| 字段           | 类型     | 说明                                          |\n| -------------- | -------- | --------------------------------------------- |\n| `moduleId`     | string   | 唯一标识：注册表、权限前缀、审计日志都用它    |\n| `prismaSchema` | string?  | schema 路径，填了就自动链接到主 schema        |\n| `requires`     | string[] | 依赖的模块 ID，只声明顺序与前置，不给代码访问 |\n\n## 边界规则\n\nimport 由 `verify-module.mjs` 强制校验。\n\n**允许：**\n\n- `@be-water/module-sdk` — shared 契约\n- `@be-water/module-sdk/server` — server 契约与运行时（仅 server 侧）\n- `@be-water/module-sdk/client` — client 契约与运行时（仅 client 侧）\n- `@be-water/ui` — UI 原语\n- 第三方库（react、react-router、lucide-react 等）\n\n**禁止：**\n\n- `@be-water/server-kernel` / `@be-water/client-kit` / `@be-water/shared` — 一律经\n  module-sdk 间接访问，内核的内部结构不该成为模块的依赖面\n- 其它模块的包 — 跨模块走扩展点，不走 import\n- `apps/*` 里的任何代码\n\n## 接入流程\n\n```bash\n# 1. 把模块包放进 modules/<moduleId>/\n\n# 2. 装依赖 + 生成注册表\npnpm install\npnpm gen:external-modules\n\n# 3. gen 脚本可能给 apps 补了 workspace 依赖，再装一次\npnpm install\n\n# 4. 有 prisma schema 时：生成 client + 迁移\npnpm --filter server exec prisma generate\npnpm --filter server exec prisma migrate dev --name <module_name>\n\n# 5. 校验\npnpm check:modules\npnpm check:deps\npnpm typecheck\n```\n\n### gen:external-modules 做了什么\n\n1. 发现 `modules/*` 下的模块包\n2. 生成 `apps/{server,client}/src/external-modules.ts` 两份注册表\n3. 把模块的 Prisma schema 符号链接到 `apps/server/prisma/models/<id>.prisma`\n4. 登记租户列与模块清单\n5. 给 `apps/{server,client}/package.json` 补上 `workspace:*` 依赖\n\n## Prisma schema\n\n外部模块只声明模型，迁移统一由 `apps/server` 生成和管理——**一个数据库只有一条迁移\n历史**，各模块各管一段的话，部署顺序会立刻变成没人算得清的问题。\n\n```prisma\nmodel MyEntity {\n  id         String   @id @default(uuid())\n  tenant_id  String\n  name       String\n  created_at DateTime @default(now())\n\n  @@index([tenant_id])\n}\n```\n\n`tenant_id` 不是可选项：没有它的表会被模块契约校验挡下来。\n\n## 下一步\n\n- 模块的整体设计 → [模块化架构](/docs/modular-architecture)\n',
  },
  {
    locale: "zh-CN",
    filename: "installation.md",
    raw: "---\ntitle: 安装与部署\ndescription: 构建、环境变量、Docker 镜像与单租户部署\ncategory: getting-started\nsort_order: 10\n---\n\n从构建到上线的完整流程，以及生产环境最容易配错的那几项。\n\n## 构建与启动\n\n```bash\npnpm build\npnpm start\n```\n\n`build` 并行编译两端：client 走 Vite 出静态资源，server 走 esbuild 打成单文件\n`apps/server/dist/index.js`。`start` 就是 `node dist/index.js`。\n\n> server 是**单文件 bundle**，运行时不会去读源码目录里的旁路文件。需要随代码走的\n> 内容（站点 CSS、默认租户的文档）都在构建期内联进产物，靠的是各自的 `assemble`\n> 脚本——新增这类资源时照着做，别在运行时 `readFile` 相对路径。\n\n## 环境变量\n\n完整清单见仓库根的 `.env.example`。上线必须想清楚的几项：\n\n| 变量                           | 说明                                             |\n| ------------------------------ | ------------------------------------------------ |\n| `DATABASE_URL`                 | PostgreSQL 连接串                                |\n| `JWT_SECRET`                   | 至少 32 字符的随机串                             |\n| `TENANT_SECRET_ENCRYPTION_KEY` | 租户密钥加密主密钥，32 字节 hex（`openssl rand -hex 32`） |\n| `FRONTEND_URL`                 | 产品站地址                                       |\n| `PLATFORM_URL`                 | 平台控制台地址，**必须与 `FRONTEND_URL` 不同 Host** |\n| `TENANT_BASE_DOMAIN`           | 租户子域基域（如 `example.com`）                 |\n| `SINGLE_TENANT`                | `true` 时启用单租户模式                          |\n\n### 为什么两个 URL 必须不同 Host\n\n分流是按 Host 做的，不是按路径。两者同 Host 时控制台与产品站会互相抢同一个入口，\n表现就是「访问 `/platform` 被无限重定向」。反向代理也依赖这一点：平台 Host 直接发\n静态 SPA，其余 Host 的 HTML 反代给 Marketing SSR。\n\n## Docker\n\n镜像定义在 `docker/Dockerfile`，三段式：`builder` 装依赖并构建，`app` 是 Node API\n进程，`web` 是带静态资源的 Nginx。\n\n```bash\n# 本地用生产镜像验证一遍完整链路\npnpm docker:stack:up\npnpm docker:stack:logs\npnpm docker:stack:down\n```\n\n`docker/entrypoint.sh` 在启动前自动检测迁移基线并执行 `prisma migrate deploy`，**新\n环境不需要手工跑迁移**。非容器部署时手工执行一次：\n\n```bash\npnpm --filter server exec prisma migrate deploy\n```\n\n## 首次启动会自动做的事\n\nserver 起来后会先跑一遍幂等的初始化：建默认租户、给它铺一套已发布的起步站点与文档\n库、建平台系统管理员。所以一个空库部署完就能直接访问，不需要额外的 seed 步骤。\n\n## 单租户部署\n\n面向只服务一个客户的私有部署：\n\n```bash\nSINGLE_TENANT=true\n```\n\n此时保留 Tenant 模型但禁止新建租户，自助注册与 OAuth 首次登录一律并入默认租户，平台\n侧的租户管理入口隐藏，租户侧文案不出现「租户」字样。\n\n**门禁：** 改动生产 env 透传后跑一次 `pnpm check:prod-app-env`。\n\n## 反向代理\n\n租户站点通过子域（`{slug}.{TENANT_BASE_DOMAIN}`）或自定义域名接入。反代需要把这些\nHost **全部**转发到同一个后端进程，并保留原始 `Host` 头——租户就是靠它解析出来的。\n\n参考配置见 `docker/nginx/default.conf.template`。\n\n## 下一步\n\n- 入口与分流细节 → [Host 分流机制](/docs/host-routing)\n- 租户与域名管理 → [租户管理](/docs/tenant-management)\n",
  },
  {
    locale: "zh-CN",
    filename: "manage-docs.md",
    raw: "---\ntitle: 文档库管理\ndescription: 文档的创建、发布、多语言与导入导出\ncategory: build-operate\nsort_order: 70\n---\n\n每个站点有一个文档库，对外暴露在 `/docs`（索引）与 `/docs/…`（详情）。你正在读的\n这一篇就存在里面。\n\n## 一篇文档是什么\n\n「标题 + Markdown 正文 + 分类」，不进页面版式体系：\n\n| 字段     | 说明                                           |\n| -------- | ---------------------------------------------- |\n| 路径     | URL 路径段（`/docs/<路径>`）                   |\n| 语言     | 一篇译文一行，与路径共同构成文档的身份         |\n| 标题     | 标题                                           |\n| 摘要     | 进 SEO 与列表                                  |\n| 正文     | Markdown 正文                                  |\n| 分类     | 索引页按它分组                                 |\n| 排序权重 | 全局升序                                       |\n\n**版式**归两张文档模板页（文档索引 / 文档详情）——正文写作和版式编排是两件事，分开做。\n\n## 草稿与发布\n\n与页面同口径：\n\n- 编辑器改的是**草稿**\n- 发布把草稿复制到**线上**，访客才看得到\n- 草稿的改动可以「撤销」，把线上内容回灌回来\n- 取消发布后文档不再出现在 `/docs` 里，但正文还在\n\n工作流：编辑 → 预览 → 发布。\n\n## 创建与编辑\n\n站点管理 → 文档库：\n\n1. 点右下角悬浮按钮新建\n2. 填标题、路径、语言、分类、正文\n3. 保存（写草稿）\n4. 在行操作里点「发布」\n\n编辑器支持全屏分屏预览：左边写 Markdown，右边实时渲染。`⌘S` / `Ctrl+S` 保存。\n\n## 路径规则\n\n- 单段：字母数字开头结尾，中间可含连字符\n- 全小写，最长 63 字符\n- 例：`quickstart`、`api-reference`、`faq`\n\n路径在编辑器里建好之后就锁定了——改它等于让所有外链和搜索结果失效。要换路径就新建\n一篇，再把老的取消发布。\n\n## 多语言\n\n同一路径每种语言**各存一行**，互不覆盖，各自有独立的草稿与发布状态——同路径就是\n翻译组的 key（与页面的「同路径 = 互为译文」同口径）。新建时选语言；建好之后不能改\n语言——那等于把这一行搬进另一组译文，而那边可能已经有同路径的文档了。\n\n要给一篇已有文档补译文：行菜单里点「复制到其它语言」。路径固定沿用，正文先拷一份\n当翻译起点，复制出来的是草稿，译完再发布。\n\n前台按访客语言取对应版本：\n\n- 主语言不带 URL 前缀，其余语言走 `/{locale}/docs`\n- 请求的语言**一篇文档都没有**时，整库回落主语言，而不是给一个空目录或 404\n- 某篇文档缺这门语言时，那一篇回落主语言\n- 页头的语言切换器（页头设置里的 `show_locale_switcher`）只列出**已发布**的同路径\n  译文；开了开关但还没有第二门语言的已发布行时，切换器不会出现\n\n文档库有多种语言时，管理端的列表会多出一列「语言」和一组语言筛选；只写了一种语言时\n两者都不显示。\n\n## 分类与排序\n\n- 分类是**站点级实体**：每种语言各写各的显示名，但共用同一个**分类标识**（key）。\n  各语言版本的同一篇文档应选同一个分类，前台会按当前语言显示对应名称。\n- 在文档编辑器里可「管理分类」：新建标识、填写各语言显示名；文档里用下拉选择分类。\n- 索引页按分类分组；**分类栏的顺序**由 `MarketingDocCategory.sort_order` 决定（管理分类时可调整）\n- 同分类内按文档排序权重升序，再按标题\n- 分类留空即**不归类**：这些文档直接列在目录顶层、恒排最后，不会被塞进一个叫「其它」\n  的分类里。不必为了填满而分类——一个只有一篇文档的分类，那行抬头什么也没分开\n- 全部文档只有一个分类时，侧栏目录与页头下拉都不画分类抬头\n\n导入 / 导出 frontmatter 里的 `category` 填**分类标识**（如 `getting-started`），须先在「管理分类」里建好。\n\n## 导入与导出\n\n支持 `.md` 文件批量进出。文件名即路径，frontmatter 提供元数据（字段名按导入格式）：\n\n```markdown\n---\ntitle: 文档标题\nslug: quickstart\nlocale: zh-CN\ndescription: 一句话摘要\ncategory: getting-started\nsort_order: 10\n---\n\n正文内容……\n```\n\n- **导入**：同路径 + 同语言已存在则覆盖草稿（导入的目的就是更新内容）。已发布的\n  文档导入后需要再点一次发布才上线。frontmatter 里**没写** `sort_order` 时不动既有\n  排序——排序多半是在编辑器里拖出来的，不该被一次导入重置。\n- **导出**：单篇或全部，导出的是草稿内容，与编辑器所见一致。\n\n语言由文件自己带：frontmatter 的 `locale` 优先，其次是文件名后缀（`faq.en.md`），\n两处都没有就落到站点主语言。导出时主语言用 `faq.md`，其余语言用 `faq.en.md`——所以\n「导出全部」在多语言库上不会下出两个重名文件，导出再导入也是闭环的。\n\n## 下一步\n\n- 调整文档页版式 → [页面与版式](/docs/manage-pages)\n- 配置导航入口 → [建站与主题](/docs/build-site)\n",
  },
  {
    locale: "zh-CN",
    filename: "manage-pages.md",
    raw: "---\ntitle: 页面与版式\ndescription: Section 系统、分栏、草稿发布与页面预设\ncategory: build-operate\nsort_order: 60\n---\n\n站点的每一页由若干 section（区块）按顺序拼成。这一篇讲怎么编排它们。\n\n## Section 一览\n\n| Section        | 中文名     | 用来做什么               |\n| -------------- | ---------- | ------------------------ |\n| `header`       | 页头       | 站点级，所有页面共用     |\n| `footer`       | 页脚       | 站点级，所有页面共用     |\n| `hero`         | 首屏 Hero  | 首屏主张 + 主按钮        |\n| `page-header`  | 页面标题   | 内页的标题区             |\n| `feature-grid` | 特性网格   | 带图标的卖点网格         |\n| `cards`        | 卡片组     | 卡片形式的内容组         |\n| `steps`        | 步骤       | 有先后的流程             |\n| `spec-list`    | 规格表     | 参数 / 规格的键值列表    |\n| `pricing`      | 定价套餐   | 定价表                   |\n| `faq`          | 常见问题   | 折叠问答                 |\n| `band`         | 通栏 CTA   | 一条横贯的行动号召       |\n| `prose`        | 富文本     | 直接写 Markdown          |\n| `form`         | 表单       | 收集线索，提交进表单记录 |\n| `page-menu`    | 页面菜单   | 页面级的目录 / 子导航    |\n| `group`        | 分栏       | 容器：把 section 并排放  |\n| `doc-list`     | 文档列表   | 文档索引                 |\n| `doc-nav`      | 文档导航   | 文档侧边目录             |\n| `doc-toc`      | 章节导航   | 当前文档的章节锚点       |\n| `doc-article`  | 文档正文   | 文档正文本体             |\n\n文档相关的四个区块只在文档模板页里有意义——它们吃的是文档库的数据。\n\n在页面编辑器里拖拽排序、增删、逐个改设置。\n\n## 分栏\n\n想让两块内容并排，加一段「分栏」，再往每一列里放 section。\n\n- **列数**：在左侧结构树里往这一段加 / 删「列」，最多 4 列\n- **列宽**：选中分栏段，拖「列宽」上的分栏点。一行共 12 份，拖的是列与列之间的界线，\n  所以宽度永远加起来是一整行——不会出现配到一半剩下一条空白\n- **分隔线**：选中某一列，勾「右侧分隔线」，就在它和右边那一列之间画一条竖线，高度\n  铺满整行（最后一列没有，它右边没有要分开的东西）。线型、粗细、颜色逐列配，颜色\n  留空则跟随主题边框色\n\n窄屏一律上下堆叠，列宽不生效；谁排在最前由每列的「堆叠顺序」决定。\n\n## 草稿与发布\n\n每个页面有两份内容：\n\n- **草稿**：编辑器正在改的那一份，访客看不到\n- **线上**：已发布的那一份，访客看到的\n\n发布 = 把草稿复制到线上；撤销 = 把线上回灌进草稿。列表里的「有未发布改动」标记就是\n两者不一致。\n\n这个语义和文档库完全一致：先改草稿，确认无误再发布，半成品不会暴露给访客。发布过的\n页面还会留下版本记录，可以回看是谁在什么时候改的。\n\n## 页面预设\n\n新建页面时可以选一个预设起步，它是排好的区块组合：\n\n- 首页\n- 定价页\n- 关于页\n- 联系页\n\n另外还有两张**文档模板页**（文档索引 / 文档详情）。它们不出现在页面列表里，只有你想\n改文档页的版式时才需要建；没建时前台用内置版式渲染。\n\n## 页面路径\n\n页面的访问路径由路径字段决定：首页固定为 `/`，其它页面例如 `pricing` → `/pricing`。\n\n> `/docs` 是文档库的专属路径，不能拿来当页面路径。语言代码（`en`、`zh-CN`）同理，\n> 它们在 URL 上占的是同一个位置。\n\n## 页面顺序\n\n页面列表里的先后就是访客看到的先后：页头的「全部页面」导航、页面菜单与 sitemap 都按\n它排。用每行的上移 / 下移调整，改完立即生效，不需要发布。\n\n同一个页面的各语言版本算**一组**，一起挪——它们在导航里本来就是同一个位置。\n\n> 列表处于筛选状态时上下移会收起来：那时看到的先后不是真实先后，排了也是盲排。先清\n> 空筛选再排。\n\n## 页面状态与可见性\n\n- **状态**：草稿（访客不可见）或已发布。新建页面默认是草稿。\n- **可见性**：所有人可见，或仅站点会员可见。选「仅会员」时，未登录访客拿到的是不含\n  正文的骨架，正文在登录后才补上——不是靠前端藏起来。\n\n## SEO\n\n页面的标题与摘要进 SEO 元信息。详情页（文档）的 SEO **跟着内容本身走**而不是跟着\n模板页：每篇文档的标题、摘要各不相同，套模板页那一份只会让搜索结果里几十条长得一样。\n\n## 下一步\n\n- 管理文档内容 → [文档库管理](/docs/manage-docs)\n- 配置会员门控 → [会员系统](/docs/members)\n",
  },
  {
    locale: "zh-CN",
    filename: "members.md",
    raw: "---\ntitle: 会员系统\ndescription: 站点会员、登录方式与会员专属内容\ncategory: build-operate\nsort_order: 80\n---\n\n会员是**站点访客**的账户体系，与工作台里的用户是两套完全独立的身份。\n\n## 会员 vs 用户\n\n|          | 会员（Member）      | 用户（User）      |\n| -------- | ------------------- | ----------------- |\n| 是谁     | 站点访客、终端客户  | 站点的运营者      |\n| 入口     | `/member/*`         | `/app/*`          |\n| 登录页   | `/member/login`     | `/login`          |\n| 能做什么 | 看会员专属内容      | 管理站点与数据    |\n| 在哪管   | 站点管理 → 站点会员 | 管理后台 → 用户   |\n\n两套身份的会话互不影响：一个人可以同时是某站的会员和另一套系统的管理员。\n\n## 开通\n\n会员是按租户开通的能力，**默认关闭**。没开通时页头不会出现登录 / 账户入口——挂一个\n点进去 403 的按钮比没有按钮更糟。\n\n开通后页头自动多出会员入口，具体展示形态在页头设置里调。\n\n## 登录方式\n\n会员通过 `/member/login` 登录，凭据方式由租户配置：邮箱密码，或 OAuth（GitHub、\nGoogle、Microsoft 等）。\n\nOAuth 需要在平台侧配好应用凭据。工作台与站点会员**共用同一条 IdP 回调地址**，回调时\n按 state 分流到对应的身份体系——两套身份不需要各注册一个 OAuth 应用。\n\n## 会员专属内容\n\n在页面的可见性设置里选「需要会员」：\n\n- 未登录访客拿到的是**不含正文**的页面骨架，正文根本没发出去\n- 登录会员由前端补齐正文\n- 搜索引擎只能索引到公开部分\n\n关键点是「服务端不发」而不是「前端藏起来」——CSS 隐藏的付费内容在浏览器里点两下就\n看得到。\n\n## 会员管理\n\n站点管理 → 站点会员：查看名单、启用 / 停用、查看活动记录。\n\n会员登录后可以在 `/member/account` 自助管理个人信息与密码。\n\n## 什么时候需要会员\n\n适合有内容门控需求的站点：知识库、课程、仅会员可下载的资源、留资后解锁的内容。\n\n纯展示型站点（落地页、官网）不需要——保持关闭即可。\n\n## 下一步\n\n- 配置页面可见性 → [页面与版式](/docs/manage-pages)\n- 工作台的用户与权限 → [用户与权限](/docs/users-permissions)\n",
  },
  {
    locale: "zh-CN",
    filename: "modular-architecture.md",
    raw: "---\ntitle: 模块化架构\ndescription: 内置模块、外部模块、注册表与模块契约校验\ncategory: core-concepts\nsort_order: 40\n---\n\n功能以**模块**为单位组织，按需启用。`apps/server` 与 `apps/client` 是极薄的组装层，\n业务逻辑全部在模块包里。\n\n## 一个模块由什么组成\n\n| 部分      | 放什么                                   |\n| --------- | ---------------------------------------- |\n| `shared/` | 跨端契约：类型、校验、纯逻辑             |\n| `server/` | 路由 + 业务逻辑 + 该模块的 Prisma schema |\n| `client/` | 页面 + 路由 + i18n                       |\n\n每个模块用 `module.ts` / `module.tsx` 实现 `ServerAppModule` / `ClientAppModule`\n接口，向内核注册路由、导航、权限与文案。\n\n## 内置模块\n\n在 `packages/builtin/*`，随平台一起发布：\n\n| 模块                      | 职责                                     |\n| ------------------------- | ---------------------------------------- |\n| `user`                    | 用户与 JWT 认证                          |\n| `rbac`                    | 角色与权限                               |\n| `platform`                | 平台控制台：租户、套餐、配额             |\n| `marketing`               | 站点 CMS：页面版式、文档库、会员入口     |\n| `site-member`             | 站点会员身份                             |\n| `billing`                 | 订阅与付款                               |\n| `audit`                   | 审计日志                                 |\n| `notification`            | 站内通知                                 |\n| `background-job`          | 任务中心                                 |\n| `error-log` / `slow-query`| 可观测性                                 |\n| `dashboard`               | 工作台卡片聚合                           |\n\n## 外部模块\n\n第三方与定制功能放在仓库根的 `modules/*`（示例：`note`、`todo`、`bookmark`）：\n\n- 只通过 `@be-water/module-sdk` 门面与内核交互\n- import 边界由 `verify-module.mjs` 强制校验\n- 自带 Prisma schema，迁移统一由 `apps/server` 管理\n\n接入流程见 [安装外部模块](/docs/install-external-module)。\n\n## 模块间不许直接 import\n\n一个模块不能 `import` 另一个模块的代码，跨模块协作走内核提供的扩展点：事件总线、\nProvider、Slot。`requires` 只声明依赖关系与加载顺序，不给代码级访问。\n\n理由是单向依赖分层：内核与基础设施不认识业务模块，业务模块之间也不互相认识，任何一个\n都能单独删掉而不牵动其它。\n\n## 注册表\n\n启用哪些模块由两份注册表决定：\n\n| 注册表          | 路径                                 |\n| --------------- | ------------------------------------ |\n| Server 启用模块 | `apps/server/src/enabled-modules.ts` |\n| Client 启用模块 | `apps/client/src/enabled-modules.ts` |\n\n顺序是 `基础设施 → 外壳 → 业务域`，保证依赖先于使用者加载。外部模块那一段由\n`pnpm gen:external-modules` 生成，不要手工改。\n\n## 路由前缀是硬约束\n\n新模块的 `renderRoutes` / `nav.path` / `mobileTabPaths` **必须**用 `/app/<模块>`\n前缀（如 `/app/site`、`/app/notes`）。挂在顶层的路由会在租户域名上被 CMS 吃掉。\n原因见 [Host 分流机制](/docs/host-routing)。\n\n## 校验与生成\n\n```bash\npnpm gen:module <spec.yaml>          # 由 spec 生成模块骨架\npnpm check:modules                   # 注册表 / 租户列 / 开关 / 权限 / 排序 / 外壳 / nav / import 边界\npnpm check:deps                      # 循环依赖检测\nnode scripts/verify-module.mjs <id>  # 只查单个模块\n```\n\n标准路径是：填 spec → `gen:module` → 补业务逻辑 → `check:modules`。\n",
  },
  {
    locale: "zh-CN",
    filename: "multi-tenant.md",
    raw: '---\ntitle: 多租户架构\ndescription: 租户隔离、数据范围、默认租户与单租户模式\ncategory: core-concepts\nsort_order: 30\n---\n\n平台从设计之初就是多租户的：一套部署服务多个互不干扰的租户，每个租户有自己的站点、\n用户和数据。\n\n## 什么是租户\n\n租户（Tenant）是数据隔离的顶层边界，一个租户对应一个组织或客户，拥有：\n\n- 自己的营销站点（页面、文档、主题）\n- 自己的用户与角色权限\n- 自己的站点会员\n- 自己的业务数据（按 `tenant_id` 隔离）\n\n> 在租户侧与公开面，界面文案**不出现**「租户」「Tenant」字样——对终端用户来说，它就\n> 是「站点」或「组织」。这不是措辞洁癖：同一套代码要能作为单客户私有部署交付，那时\n> 「租户」是一个客户根本不该看见的内部概念。\n\n## 数据隔离\n\n所有业务表都带 `tenant_id` 列，查询统一走 `withTenantScope(tenant_id)` 注入租户范围：\n\n```typescript\nconst records = await prisma.marketingDoc.findMany({\n  where: withTenantScope(tenant_id, { status: "published" }),\n});\n```\n\n隔离靠**统一的查询入口**保证，而不是靠每个调用点自觉。绕过它直接写 `where` 会被\n模块契约校验挡下来。\n\n## 默认租户\n\nslug 为 `default` 的租户是产品主域隐式绑定的组织。访问 `FRONTEND_URL`（本地\n`localhost`）看到的就是它的站点。\n\n它的站点与文档库**在服务端启动时自动初始化**，幂等且按语言分别判断：某种语言已经有\n已发布文档就跳过，不覆盖后续编辑。所以升级部署不会把你改过的首页冲掉，而新增一门\n语言的初始内容会在下次启动时自动补上。\n\n## 租户的创建与管理\n\n新租户在平台控制台（`PLATFORM_URL`，本地 `127.0.0.1`）创建。新建出来的租户是\nactive 的，但**站点内容为空**——自动初始化只针对默认租户。让它有内容有两条路：租户\n管理员自己登录工作台应用起步模板，或由平台管理员代为应用。\n\n## 单租户模式\n\n面向私有部署，设 `SINGLE_TENANT=true`：\n\n- 隐藏平台控制台与租户管理入口\n- 禁止新建租户；自助注册与 OAuth 首次登录一律并入默认租户\n- 界面上不暴露多租户概念\n\n单租户与多租户**共用同一套代码**，区别只是运行时配置——不需要为私有部署维护单独的\n分支。\n\n## 路由由 Host 决定\n\n- `FRONTEND_URL` → 默认租户\n- `{slug}.{TENANT_BASE_DOMAIN}` → 对应 slug 的租户\n- 租户自定义域名 → 绑定的那个租户\n\n详见 [Host 分流机制](/docs/host-routing)。\n',
  },
  {
    locale: "zh-CN",
    filename: "tenant-management.md",
    raw: "---\ntitle: 租户管理\ndescription: 平台控制台里的租户、配额、功能开关与域名\ncategory: platform-admin\nsort_order: 100\n---\n\n租户管理在**平台控制台**进行，不在租户工作台。只有平台管理员能进。\n\n## 进入控制台\n\n本地是 `http://127.0.0.1:7300/platform`，未登录会转到 `/login`。\n\n> 必须是 `127.0.0.1` 而不是 `localhost`——后者是产品站入口，在那儿访问 `/platform`\n> 会被送回控制台 Host。详见 [Host 分流机制](/docs/host-routing)。\n\n生产环境对应的是 `PLATFORM_URL` 那个 Host。\n\n## 租户列表\n\n控制台列出所有租户：状态、创建时间、用量统计、启用的模块。支持按状态筛选、按名称\n搜索。\n\n## 创建租户\n\n新建时要填：\n\n- **slug** — 用于子域 `{slug}.{TENANT_BASE_DOMAIN}`，建好之后是对外地址的一部分\n- **名称** — 展示用\n- **默认语言** — 站点主语言，决定哪种语言的 URL 不带前缀\n\n新租户默认 active，但**站点内容为空**——自动铺内容只发生在默认租户身上。让它有内容\n有两条路：租户管理员自己登录工作台应用起步模板，或平台管理员代为应用。\n\n## 配额\n\n每个租户有一组配额上限：用户数、页面数、文档数、存储用量等。配额按套餐档位预设，\n也可以为单个租户单独调整。\n\n## 功能开关\n\n功能按权益（entitlement）控制：哪些模块可用、能不能开会员、能不能绑自定义域名……\n它与计费套餐联动，升级套餐解锁更多能力。\n\n## 自定义域名\n\n1. 租户在自己的 DNS 里把域名 CNAME 到平台\n2. 平台管理员在控制台把该域名登记到这个租户\n3. 平台验证归属后生效\n\n绑定后该域名指向这个租户的站点，路由行为与子域完全一致。\n\n## 停用租户\n\n停用（suspend）后：站点对访客不可见、管理员无法登录工作台、**数据完整保留**，随时可\n恢复。\n\n彻底删除是另一个操作，且不可恢复。日常清理优先用停用。\n\n## 审计\n\n所有租户管理操作——创建、停用、改配额、绑域名——都会记审计日志，在控制台的审计日志\n里按模块、动作、时间筛选查看。\n\n## 下一步\n\n- 租户内部的用户与权限 → [用户与权限](/docs/users-permissions)\n- 单租户私有部署 → [安装与部署](/docs/installation)\n",
  },
  {
    locale: "zh-CN",
    filename: "users-permissions.md",
    raw: '---\ntitle: 用户与权限\ndescription: 用户管理、角色与 RBAC 权限体系\ncategory: platform-admin\nsort_order: 110\n---\n\n这一篇讲租户内部的用户与权限。这里的「用户」指工作台的使用者，不是站点会员——两者的\n区别见 [会员系统](/docs/members)。\n\n## 用户管理\n\n管理后台 → 用户：\n\n- 创建用户（用户名 + 初始密码）\n- 编辑资料、启用 / 停用、重置密码\n- 分配角色\n\n用户登录后进入 `/app/*`，能看到哪些模块由权限决定。\n\n## 权限怎么算出来的\n\n权限有两条来源，只有两条：\n\n1. **系统管理员**标记：勾了这个标记的用户，直接拥有本租户的全部权限，不需要分配任何\n   角色。每个租户至少要有一个，否则没人能授权别人。\n2. **角色**：其余用户的权限**完全**来自分配到的角色。没有角色 = 没有任何权限。\n\n新租户不会预置角色——预置的「编辑者」在每家的含义都不一样，与其猜，不如让第一位管理\n员按自己的分工建。\n\n## 权限命名\n\n权限按 `<模块>.<动作>` 命名，由各模块自己声明并汇总成权限目录：\n\n| 权限          | 含义         |\n| ------------- | ------------ |\n| `site.read`   | 查看站点内容 |\n| `site.write`  | 编辑站点内容 |\n| `user.manage` | 管理用户     |\n| `role.manage` | 管理角色     |\n| `audit.read`  | 查看审计日志 |\n\n只有权限目录里存在的键才生效——手写一个不存在的权限不会报错，但也永远不会命中，\n所以建角色时从目录里勾，别手打。\n\n## 角色管理\n\n管理后台 → 角色：查看权限目录、建角色并勾权限、改权限、删角色（已分配出去的要先\n解绑）。\n\n## 权限检查\n\n前端：\n\n```tsx\nconst { hasPermission } = usePermissions();\nif (hasPermission("site.write")) {\n  // 显示编辑按钮\n}\n```\n\n无权限的入口**直接不渲染**，不是置灰——一个点不动的按钮只会让人反复来问为什么。\n\n后端：\n\n```typescript\napp.get("/api/site/docs", { preHandler: [app.requirePermission("site.read")] }, handler);\n```\n\n后端校验是唯一真正生效的那道：前端隐藏 UI 只是体验，任何人都能直接调接口。\n\n## 审计日志\n\n所有写操作都记审计：谁、什么时候、对哪个资源、做了什么。在管理后台的审计日志里按\n模块、动作、时间筛选。\n\n新模块的写操作也要记——这是模块契约校验的检查项之一，漏了 `check:modules` 会报错。\n\n## 与会员的区别\n\n|          | 用户（User）    | 会员（Member）      |\n| -------- | --------------- | ------------------- |\n| 是谁     | 站点运营者      | 站点访客            |\n| 入口     | `/app/*`        | `/member/*`         |\n| 权限     | RBAC 角色权限   | 有 / 无会员身份     |\n| 在哪管   | 管理后台 → 用户 | 站点管理 → 站点会员 |\n',
  },
];
