---
title: 页面与版式
description: Section 系统、草稿发布与页面预设
category: 站点管理
---

# 页面与版式

站点的每一页由若干 section（区块）按顺序拼成。本篇介绍 section 系统、草稿/发布语义，以及如何编排页面。

## Section 是什么

Section 是页面的组成单元，每个 section 有自己的类型与设置：

| Section       | 作用                       |
| ------------- | -------------------------- |
| `header`      | 页头（站点级）             |
| `footer`      | 页脚（站点级）             |
| `hero`        | 首屏大图                   |
| `band`        | 通栏内容带                 |
| `cards`       | 卡片网格                   |
| `steps`       | 步骤列表                   |
| `faq`         | 常见问题折叠列表           |
| `pricing`     | 定价表                     |
| `form`        | 表单（收集线索）           |
| `prose`       | 内联 markdown 正文         |
| `doc-source`  | 引用平台文档（代码版本化） |
| `doc-list`    | 文档库索引                 |
| `doc-nav`     | 文档侧边导航               |
| `doc-toc`     | 文档目录                   |

在页面编辑器里拖拽排序、增删 section、调整每个 section 的设置。

## 草稿与发布

页面有两份内容：

- **草稿**（`_draft`）：编辑器正在改的那一份，访客看不到
- **线上**：发布后访客看到的那一份

发布时把草稿复制到线上；撤销时反向回灌。`content_dirty` 标记表示草稿与线上一致与否。

这个语义和文档库一致——先改草稿，确认无误再发布上线，避免半成品暴露给访客。

## 页面预设

页面预设（Preset）是预排好的 section 组合，用来快速建页：

- `home` — 标准首页
- `pricing` — 定价页
- `about` — 关于页
- `contact` — 联系页

新建页面时选一个预设起步，再按需调整。

## 页面路径

每个页面有自己的 slug，访问路径是 `/{slug}`：

- `home` → `/`
- `pricing` → `/pricing`
- `about` → `/about`

> `/docs` 是文档库的专属路径，不能用作页面 slug。文档库由独立的文档系统管理，不进页面版式体系。

## 页面状态

- `published` — 已发布，访客可见
- `draft` — 草稿，访客不可见

新建页面默认是草稿，需要手动发布才上线。

## SEO

页面的 `title` 与 `description` 进 SEO 元信息。详情页（如文档）的 SEO 跟着内容本身走，不是跟着模板页——每篇的标题/摘要各不相同。

## 下一步

- 管理文档内容 → [文档库管理](/docs/manage-docs)
- 配置会员入口 → [会员系统](/docs/members)
