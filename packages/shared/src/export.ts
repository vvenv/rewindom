/** 同步下载 / 任务完成后拉取文件时的 HTTP 超时（毫秒） */
export const EXPORT_DOWNLOAD_TIMEOUT_MS = 10 * 60 * 1000;

/** 数据备份空闲超时（毫秒） */
export const DATABASE_BACKUP_IDLE_TIMEOUT_MS = 25 * 60 * 1000;

/** 数据还原空闲超时（毫秒） */
export const DATABASE_RESTORE_IDLE_TIMEOUT_MS = 25 * 60 * 1000;
